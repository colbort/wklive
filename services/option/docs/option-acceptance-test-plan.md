# Option 生产验收测试计划

## 1. 目标与退出条件

本计划验证资金守恒、风险准入、交易完整性、行权、到期结算、故障恢复和运营可执行性。生产退出条件：

1. 适用 P0 全部为 `DONE`；卖方、美式、实物、组合保证金各自依赖的 P1 全部为 `DONE`。
2. 自动化单测、`go test -race`、`go vet`、迁移重复执行和 API/UI 类型检查通过。
3. 下表所有适用场景通过，无开放 SEV-1/2 缺陷。
4. Option 账、Asset 流水和余额差额为 0。
5. 技术、风控、清算和运营签字；生产参数另按上市检查表签字。

## 2. 环境与证据

- 独立预生产租户，时间可控的合约和权威行情快照源。
- MySQL、Redis、消息队列、Option、Asset、Market 使用生产同版本配置。
- 测试用户至少覆盖两个 `account_id`、多/空双方、费用/保险/兜底账户。
- 每个用例保存：输入参数、数据库前后快照、RPC 响应、消息、日志、Asset 流水、余额、告警和清理结果。
- 金额断言使用 DECIMAL 精确比较，不以浮点误差忽略非零差额。

## 3. 验收矩阵

| 用例 | 场景 | 核心断言 | 对应整改 |
| --- | --- | --- | --- |
| ACCT-001 | 同用户两个账户持有卖方头寸 | 只形成一个钱包风险权益；两账户保证金均计入 | P0-001 |
| ACCT-002 | 一个账户触发限制后另一账户下单 | 两账户均不可增加风险 | P0-001 |
| ACCT-003 | 同用户跨账户相反订单 | 不成交并记录 STP 拒绝/取消 | P0-001/P1-002 |
| EQ-001 | 多头支付权利金，标记价不变 | 权益忽略费用前不变 | P0-002 |
| EQ-002 | 空头收到权利金，标记价不变 | 权益忽略费用前不变 | P0-002 |
| EQ-003 | 多空等量同合约 | 净期权市值为 0 | P0-002 |
| EQ-004 | 买方扣款延迟 | 扣款成功前不得生成可计价多头持仓 | P0-002 |
| ASSET-001 | Option 开仓冻结在 Asset 已成功但 RPC 响应丢失；分别保留/删除幂等行后用原业务号重试，并尝试用同业务号修改金额 | 每个业务号只有一条冻结记录和一条冻结流水；迁移前无幂等行的唯一匹配冻结被安全接管；不同经济字段拒绝；释放后总额/可用/冻结及 Option `account_id=0` 镜像一致 | P0-001/P0-004 |
| ASSET-002 | 在迁移前构造一个唯一 Option 冻结键、一个重复 Option 冻结键和一个重复非 Option 键，连续执行冻结证据迁移两次并采样运营指标 | 覆盖索引唯一；唯一 Option 键补成功幂等账；重复 Option 键不补账并精确输出一个 `asset_freeze_duplicate`/最早时间；非 Option 重复键不进入该指标；OPT-A033 存在 | P0-001/P1-008 |
| COIN-003 | 分别建立实物 Call/Put 卖方活动订单，用真实 Asset 冻结订单记录的 BTC/USDT 后执行 Option 控制撤单和资产指令任务，再重跑任务 | Call 释放 BTC、Put 释放 USDT；两条指令成功且对账一致；订单由 CANCELING 到 CANCELED 且保证金归零；钱包与 `account_id=0` 镜像恢复；每币种冻结/释放各一条且重跑不增加 | P0-004 |
| MKT-001 | 只刷新标的价，标记价陈旧 | 风险检查失败，钱包 `RESTRICTED` | P0-003/P1-001 |
| MKT-002 | 只刷新标记价，标的价陈旧 | 行权及保证金准入失败 | P0-003 |
| MKT-003 | 时间戳来自未来/倒退 | 拒绝并告警 | P0-003 |
| MKT-004 | 管理端查看缺失、未来、30秒边界/过期的标的与标记价；Greeks 分别构造阈值0、批准边界、边界外1秒和未来时间 | 分别展示原始时间和状态；Greeks 阈值0显示“阈值待审批”，正数在边界内显示新鲜、超一秒显示过期 | P0-003 |
| CHAIN-001 | 同一标的/到期包含成对 Call/Put、单边缺失和其他到期合约 | 只返回精确到期；同一行权价正确配对，缺腿为空且不混入其他到期 | P2-006 |
| CHAIN-002 | PENDING/TRADING/PAUSED/EXPIRED 合约混合查询 | 默认仅 TRADING；只允许显式查询 PAUSED；未上市和历史合约不泄漏 | P2-006 |
| CHAIN-003 | 同价多用户挂单、部分成交、FUNDING、撤单和零余量混合 | 只聚合 PENDING/PART_FILLED 正余量；买降卖升；不返回用户/订单身份 | P2-006 |
| CHAIN-004 | 成交时间位于24h边界前一秒、边界和后一秒 | 边界及以后各成交只计一次；成交量/额/笔数与成交表精确一致 | P2-006 |
| CHAIN-005 | 已落仓多空相等、任务事务中断及人为构造不平衡 | 正常 OI 为单边值；不平衡返回两侧、较大侧和 `oi_balanced=false` 并告警 | P2-006 |
| CHAIN-006 | 500合约链、100档盘口并发读取及跨租户同 ID 数据 | 达到批准 SLA，无 N+1/租户泄漏；501合约和101档请求失败且不截断误导 | P2-006 |
| COMBO-001 | 2:2比例、重复合约、5腿、比例9、跨标的/到期/乘数、平仓或MMP腿 | 全部在建单/冻结前拒绝；合法比例必须约分至最低项 | P2-007 |
| COMBO-002 | 同一 clientComboId 同内容/不同内容及50路并发提交 | 同内容返回同一父单；不同内容冲突；只有一组腿、影子单和冻结 | P2-007 |
| COMBO-003 | 两条/四条腿中任一冻结失败或成功响应丢失 | 0腿入策略簿/成交；成功冻结不丢失，取消后按原业务号各释放一次 | P2-007 |
| COMBO-004 | 最优单个反向父单流动性不足、刚好足量、多一份、净价及每腿限价边界 | 首版 FOK 不跨 maker 聚合：最优单个 maker 不足即0腿；足量时全部腿按比例成交；净价和腿限价边界允许，越界一个 tick 拒绝 | P2-007 |
| COMBO-005 | 在第二腿写成交/outbox/序号/资产指令时注入失败 | 父单、所有腿订单和成交全部回滚，maker余量和撮合序号无残留推进 | P2-007 |
| COMBO-006 | 同用户跨账户提交相反策略，或影子单与单腿订单可成交 | STP阻止组合自成交；影子单不出现在单腿深度且不被单腿 matcher 消费 | P2-007 |
| COMBO-007 | 组合撮合同时触发 halt、kill switch、日历闭市、到期；建单同时触发用户/OI上限 | 规范锁序下只允许全腿成交或0腿；建单占用完整投影敞口，撮合不会释放；失败原因可审计，冻结安全释放 | P2-007 |
| COMBO-008 | 成功成交后任一腿买方扣冻结失败/重试、任务重启和响应丢失 | 同一 `comboMatchNo` 全部买方扣冻结成功前所有腿持仓事件均不可运行；业务号幂等；成功后各合约按序最终落仓且资金守恒，暂态水位差异可监控 | P2-007 |
| COMBO-009 | 不同租户/权限管理员查询父单、下钻影子单/成交/资金指令并对活动父单强撤 | 跨租户和无权限均拒绝；详情保留父腿关联、总数及截断提示；强撤必须填写原因且只调用整组状态机，全部腿按原业务号释放；不存在单腿操作权限 | P2-007 |
| COMBO-010 | 分别制造超龄 `FUNDING/CANCELING`、`MANUAL_REVIEW`、父/腿/影子不一致和缺腿成交组，并使用另一租户查询运营概览 | 指定租户五项组合水位及最早异常时间精确命中；健康租户全为0；阈值只允许10至300秒；工作台将父单异常和完整性异常标红 | P2-007 |
| COMBO-011 | 强制把影子单送入单腿 funded/match/锁后路径或盘口聚合；直接触发缺腿、重复腿号、任一买方扣款未成功的持仓事件；制造超龄屏障 | 所有路径失败关闭且0持仓；隔离/屏障 Counter 增加；超龄 Gauge 精确；完整2～4腿且全部扣款成功才可继续；`/metrics` 可抓取且规则能触发/恢复 | P2-007 |
| INST-001 | 机构规则、参与者资格和审批均未配置时搜索/调用 RFQ、大宗或做市义务入口 | 无路由、RPC、SDK 或数据库写路径；不能通过普通订单、管理员补录或 MMP 接口降级实现 | P2-008 |
| INST-002 | 非本租户、非接收人、资格过期或被暂停参与者读取/响应定向 RFQ | 全部拒绝且不泄露请求、报价、方向、数量或对手方身份；保留最小安全审计 | P2-008 |
| INST-003 | 同一 RFQ 报价被50路并发接受，或接受与撤回/到期同刻发生 | 最多一笔原子胜出；其余明确失败，资金、成交、持仓和报告只产生一次 | P2-008 |
| INST-004 | RFQ 在日历关闭、halt、行情陈旧、价格带外、超限或资金不足时接受 | 接受时重新 fail closed；不能因为报价先前有效而绕过当前门禁 | P2-008 |
| INST-005 | 大宗数量低于/等于/高于产品或逐腿最低门槛 | 低于0成交；边界及以上仅在资格、价格合理性和报告字段全部有效时允许 | P2-008 |
| INST-006 | 大宗成交时间/报告时限边界、延迟公开及更正/撤销 | 时间使用服务端权威时钟且可审计；逾期进入案件而非改时间；更正追加版本、不覆盖原记录 | P2-008 |
| INST-007 | 大宗某一腿资金/成交/outbox写入失败或 Asset 成功响应丢失 | 全部数据库腿共同回滚；异步资金按组屏障和原业务号恢复，无残腿、重复扣款或提前落仓 | P2-008 |
| INST-008 | 做市义务窗口覆盖开闭边界、halt、MMP冷静期、行情中断和已批准豁免 | 分子/分母与批准规则一致，原始序列可重放；无规则的场景不能由代码默认算作达标 | P2-008 |
| INST-009 | 修改历史委任、规则、豁免或考核结果；创建人自批 | 数据库/服务拒绝；修订和申诉追加版本并由独立人员批准 | P2-008 |
| INST-010 | RFQ/大宗/做市数据保留、监控、投诉和监管导出抽查 | 请求到成交/未成交决定链完整、租户隔离、时钟一致、字段可复核且符合批准保留期 | P2-008 |
| COIN-001 | 实物 Call 各类撤单/强撤/到期 | 全部按标的保证金币种释放且只释放一次 | P0-004 |
| COIN-002 | 实物 Put 各类撤单/强撤/到期 | 全部按结算保证金币种释放且只释放一次 | P0-004 |
| PRICE-001 | 窗口前/内/后混合样本 | 只取窗口内权威不可变样本 | P0-005 |
| PRICE-002 | 样本不足 | 保持等待并告警，不生成 0 或当前价 | P0-005 |
| PRICE-003 | 同样本乱序重放 | 结果和来源集合确定一致 | P0-005 |
| PRICE-004 | 人工更正 | 新版本、双签、原版本保留，结算只用发布版本 | P0-005 |
| PRICE-005 | 直接写入已确认但中位数、窗口、样本ID或样本数不符的版本，再运行到期任务 | 数据库拒绝新坏版本；迁移前历史坏版本在结算使用点复算失败且不创建批次，并触发 OPT-A011 | P0-005 |
| PRICE-006 | 复核人拒绝自动候选后重复运行生命周期任务；随后增加新的窗口内不可变证据 | 相同证据不生成新版本；只有证据集合或结果变化时才生成下一待复核版本 | P0-005 |
| PRICE-007 | 人工更正创建人自审、另一人复核、尝试删除旧/新版本，并查询运营水位 | 自审拒绝；独立复核成功；所有版本不可删除；合法 `manual-correction/MANUAL` 不触发证据异常 | P0-005 |
| PRICE-008 | 现金 Call 到期窗口内保存119/120/121三个不可变样本并确认中位数120；另建无正式价合约；卖方真实冻结50 USDT后运行生命周期和资产指令并重放 | 错误已确认价121由数据库拒绝；只使用正式价120；无价合约不创建结算；卖方扣冻结20并释放30、买方入账20，3条指令成功且对账，双方余额120/80、合计200、冻结0；结算/批次/合约完成且重放无重复结算或流水 | P0-005 |
| ASN-001 | 1/500/501/5000 空头指派 | FIFO 全量完成，数量和扣款守恒 | P0-006 |
| ASN-002 | 指派同时空头平仓 | 不超分配；只撤实际候选账户平仓单 | P0-006 |
| ASN-003 | 行权任务重复执行 | 不重复指派、扣款或入账 | P0-006 |
| ASN-004 | 空头被全量/部分提前行权指派 | 持仓数量与维持保证金按相同比例下降；实际保证金只随成功的 margin lot Asset 指令减少，剩余批次和冻结精确一致 | P0-006 |
| RISK-001 | 一个钱包行情坏、另一个正常 | 坏钱包限制，正常钱包同周期刷新，任务聚合报错 | P1-001 |
| RISK-002 | Asset RPC 单账户失败 | 失败钱包限制，其他钱包继续 | P1-001 |
| CTRL-001 | 用户上限剩余 10，同时并发提交 20 笔数量 1 的开仓单 | 恰好 10 笔成功；其余统一返回 `USER_LONG_LIMIT` 或 `USER_SHORT_LIMIT`，投影敞口不超过上限 | P1-002 |
| CTRL-002 | 成交已减少委托未成交量，但持仓 outbox 尚未落仓时继续下单 | 待落仓成交只计一次且仍占用限额；outbox 成功后无敞口缺口或重复计数 | P1-002 |
| CTRL-003 | 价格等于价格带上下界、越界一个 tick、标记价超过 30 秒 | 边界允许、越界拒绝为 `ORDER_PRICE_BAND`、陈旧拒绝为 `PRICE_REFERENCE_STALE`，均有审计 | P1-002 |
| CTRL-004 | 标记价跳变低于、等于、高于熔断阈值并并发下单 | 低于不触发；等于/高于原子暂停合约，新单失败，活动订单全部进入取消流程，记录原价/新价/阈值 | P1-002 |
| CTRL-005 | 用户启用 kill switch，同时存在 FUNDING/PENDING/PART_FILLED 订单并并发下单 | 启用返回前新单均被阻止或已取消；全部活动订单安全释放冻结；重复启用/撤单幂等 | P1-002 |
| CTRL-006 | 管理员解除 kill switch | 必须提供理由且只能操作授权租户；解除后可重新准入；启用/解除操作人和时间不可篡改 | P1-002 |
| CTRL-007 | 同一用户两个 `account_id` 以可成交价格对敲 | cancel-taker，不产生成交或资金流水；已有 maker 保留；记录 `SELF_TRADE_PREVENTED` | P0-001/P1-002 |
| CTRL-008 | 修改交易控制参数、合约状态或控制事件历史 | 参数/状态变更带管理员审计；控制事件 UPDATE/DELETE 被数据库拒绝 | P1-002 |
| ERR-001 | 已成交价格/数量被判定为异常成交并创建现金更正 | 原子暂停合约并撤活动单；原成交保留；案件/证据/分录不可篡改；只允许成交双方或批准的平台账户及原币种 | P1-002 |
| ERR-002 | 现金更正借贷不平、超过 16 位小数、包含无关账户或申请人自审 | 全部拒绝，不创建资金指令，不改变原资金状态 | P1-002 |
| ERR-003 | 独立管理员批准平衡现金更正 | 所有扣款为 step 1，全部成功后入账 step 2 才可运行；最终 Asset 净变化为 0，案件为 `COMPLETED` | P1-002 |
| ERR-004 | 现金更正扣款余额不足、响应丢失、重试 20 次 | 不提前入账；原指令号幂等重试；超限进入 `MANUAL_REVIEW`；人工恢复必须填写原因、携带有效操作人并追加不可变审计事件 | P1-002 |
| ERR-005 | 案件被拒绝或现金更正完成后恢复交易 | 系统不自动恢复；技术/风控确认订单、资金、通知和对账后，管理员状态变更另留审计 | P1-002 |
| MMP-001 | 未配置/禁用/已触发组提交 MMP 单，或普通单携带 group | 原子拒绝且原因明确；不冻结、不入簿、不降级成普通单 | P1-003 |
| MMP-002 | MMP 使用 LIMIT/IOC/FOK，或合法配置下使用 POST_ONLY | 非 POST_ONLY 全拒绝；POST_ONLY 仅在不主动成交时入簿 | P1-003 |
| MMP-003 | maker 成交量、笔数、买卖不利损失分别低于/等于/高于阈值 | 低于不触发；等于和高于原子转 `TRIGGERED`；损失含 maker 手续费且使用成交前标记价 | P1-003 |
| MMP-004 | 同组 101 个活动报价触发 MMP，同时存在其他组和非 MMP 单 | 当前余量事务内取消，同组全部进入取消/释放；其他组和非 MMP 单状态不变 | P1-003 |
| MMP-005 | 触发同时并发提交新报价、修改配置和人工恢复 | 锁顺序无死锁；触发/配置阶段无新单穿透；恢复前活动残单为 0 | P1-003 |
| MMP-006 | 冷静期截止前一秒、截止时刻、截止后一秒下单 | 截止前拒绝；截止及以后首单原子清零窗口并恢复，后续单正常 | P1-003 |
| MMP-007 | 组级撤单时 Asset 成功响应丢失或部分失败 | 组保持 `TRIGGERED/DISABLED`，记录 `last_error_msg`；原业务号幂等恢复，不提前重新激活 | P1-003 |
| MMP-008 | 管理员人工恢复已触发组，或尝试恢复活动/禁用组 | 已触发组带理由、清残单后恢复并审计；其他状态拒绝 | P1-003 |
| PM-001 | 未配置、待审批、已拒绝或尚未到生效时间的参数版本下提交组合卖单 | 下单原子拒绝、不冻结；存量组合风险账户扫描后转 `RESTRICTED`，不回退到内置默认参数 | P1-004 |
| PM-002 | 创建人审批自己的版本、审批非最新草案、原地修改或删除任一历史版本 | 全部拒绝；参数、状态、有效期和审计记录保持不变 | P1-004 |
| PM-003 | 独立管理员批准立即/未来生效版本 | 新版本进入 `APPROVED`；旧版本转 `SUPERSEDED` 且截止时间等于新版本生效时间；边界前后各自只解析一个版本 | P1-004 |
| PM-004 | 复制已批准历史参数创建回滚版本 | 形成更高版本和新审批记录；历史版本不被重新激活或覆盖；下单与风险扫描在生效后同时使用新 ID/版本 | P1-004 |
| PM-005 | 标的价格为 0、现价、各执行价、2 倍、5 倍及配置冲击点 | 情景损失取所有点最大值；冲击集合缺少 `-1` 或 `>=4` 时草案拒绝 | P1-004 |
| PM-006 | 同到期价差、跨到期相反头寸、跨标的相反头寸 | 仅同标的同到期组合可抵扣；跨到期和跨标的保证金分别相加 | P1-004 |
| PM-007 | 净空头名义金额低于、等于、高于集中度阈值，并提高两类附加率 | 低于/等于不收集中度附加，高于仅对超额计提；流动性附加按全部风险名义金额计提；任何附加率提高均不降低保证金 | P1-004 |
| PM-008 | 风险扫描生成组合账户快照 | 快照保存配置 ID/版本、算法、情景损失、裸空头底线、集中度和流动性附加；按该版本可确定性复算 | P1-004 |
| PM-009 | 使用生产代表性历史数据回测、极端压力和独立验证 | 数据范围、缺失处理、阈值依据、例外、结果、验证人和证据地址与批准版本一致；未签字不得开放 | P1-004 |
| PM-010 | 在参数生效边界前后提交组合保证金卖单，并尝试缺省、错版本、跨租户、非有效版本及事后改写证据 | 每笔新卖单保存创建时点唯一有效的配置 ID/版本；非法引用数据库拒绝且不插单，证据写入后不可变；普通单和组合策略逐仓影子单为 `0/0`；迁移前 `0/0` 历史卖单仍可撮合/撤单但明确标记历史证据不可用 | P1-004 |
| EX-001 | 同一客户端行权键并发 20 次 | 只有一笔行权、一次冻结，全部返回同一 ID | P1-006 |
| EX-002 | 同一键不同仓位/数量 | 后续请求拒绝，不改变仓位 | P1-006 |
| EX-003 | 非 `qty_step`、截止后、零净收益 | 均拒绝且不冻结 | P1-006 |
| EX-004 | 同一持仓依次提交 AUTO、DNE、相反指令并重放 | 版本递增、仅最新版本生效、旧版不可改写；同键同请求幂等，不同请求拒绝 | P1-006 |
| EX-005 | 内在价值低于/等于/高于阈值，分别使用 AUTO、DNE、相反指令 | AUTO 按阈值，DNE 放弃，相反指令忽略阈值；任何零/负净收益均不行权 | P1-006 |
| EX-006 | 截止前一毫秒、截止时刻、截止后一毫秒提交指令 | 仅截止前的新指令成功；截止后仍能查询并幂等重放已存在指令 | P1-006 |
| EX-007 | 分别在 PENDING、TRADING、PAUSED、EXPIRED、SETTLED、OFFLINE 状态提交主动行权/到期指令，并在事务等待期间切换状态或跨过截止 | 只有已上市且截止前的 TRADING/PAUSED 接受；PAUSED 不剥夺行权权，其余状态拒绝；事务锁后再次校验，跨界请求不落库 | P1-006 |
| EX-008 | 修改指令经济字段、将 SUPERSEDED 改回 ACTIVE、删除任一历史版本 | 数据库全部拒绝；只允许 ACTIVE→SUPERSEDED，历史版本完整保留 | P1-006/DB-002 |
| PNL-001 | 等价头寸分别平仓、提前行权、到期 | 总收益一致并等于 Asset 流水净额 | P1-005 |
| PNL-002 | 开仓、部分平仓、行权费和强平费组合 | 交易毛盈亏 + 结算毛盈亏 - 累计费用 = 总收益 = 兼容 realized_pnl | P1-005 |
| LIQ-001 | 无穿仓的逐仓整仓强平 | 用户保证金扣除+释放=接管仓位成本+强平费；保险账户接管仓位、冻结保证金和维持保证金证据同步完成 | P1-005 |
| LIQ-002 | 逐仓账户权益低于维持保证金但无需清空全部空头；覆盖恰好落在健康边界、资金前失败和任务重放 | 按`qty_step`向上选择使`equity-fee > maintenance_after`的最小数量；同钱包仅一笔；FIFO按数量比例拆分margin lot；资金前预占可用数量并标记pending，失败不得提前扣仓/扣保证金；成功后源/接管仓、收益和钱包守恒，重放无新增 | P1-005 |
| LIQ-003 | 组合保证金账户有多个跨业务账户空头；触发后配置/行情/仓位/抵押或钱包余额变化；保险基金部分覆盖、平台兜底、任意 Asset step 失败/响应丢失 | 同钱包/币种最多一笔；选择完整移除后维持保证金下降最大的空头，同值按position_id；完成资金与持仓接管后才重算并创建下一笔；执行前按当前行情/余额复核，账户恢复则取消，正常行情变化仍要求原候选降低风险并保留触发/当前剩余初始保证金较大值；事务内再次证明底线；借贷守恒且原业务号幂等恢复 | P1-005 |
| SET-001 | 分别在空头扣款调用前返回Asset不可用，以及真实Asset扣款成功落流水后丢失响应；随后恢复并重放任务 | 两种失败轮次step 1均为FAILED且step 2无成功/流水，多头不得提前入账；不确定结果已有且仅有一条扣款流水；恢复使用原指令号，不可用场景新增唯一流水、响应丢失场景认领原流水，均不得二次扣款；完成后逐币守恒，重跑无重复 | P0-005 |
| SET-002 | 同合约多头混合 AUTO/DNE，相对空头数量充足 | 只有实际行权多头收款，空头按实际行权量 FIFO 付款，双方 gross payoff 相等 | P1-006 |
| SET-003 | 现金结算空头可用余额不足，前置扣冻结成功后对可用余额扣款连续失败20次；分别以缺原因、65字符超长原因、缺操作人请求人工重试，再以独立业务号补资并受控恢复 | 第20次失败进入 `MANUAL_REVIEW`，较高step始终无成功/流水；三个非法请求均拒绝且保留`retry_count=20`；补资流水唯一，合法重试必须携带不超过64字符的原因和有效操作人；重置与审计事件同事务，事件保存原状态/次数/错误且不可修改删除；恢复沿用原指令号，最终3指令成功对账、逐币守恒、冻结0 | P0-005/P1-008 |
| PHY-001 | 实物 Call/Put 多空持仓数量需要拆分配对 | 每个单元仅含一个多头、一个空头及确定数量；单元数量汇总分别等于实值多空数量 | P1-007 |
| PHY-002 | 两个交割单元中一个买方余额不足 | 健康单元独立完成；失败单元无任何入账；整批不凭空产生资产且不无限阻塞健康用户 | P1-007 |
| PHY-003 | 买方扣款成功后空头担保扣减超时/响应丢失 | 双方入账均不运行；原指令号幂等恢复，对账后才能进入入账步骤 | P1-007 |
| PHY-004 | 补资截止前、截止时刻和截止后持续失败 | 截止前为 `CURE_REQUIRED` 并可自动重试；截止及以后转 `DEFAULTED/MANUAL_REVIEW`，停止自动重试并告警 | P1-007 |
| PHY-005 | 管理员从人工态重试失败单元 | 使用全部原指令号，不改变金额/币种/方向；成功后单元完成，历史失败原因和操作事件保留 | P1-007 |
| PHY-006 | 进程在任一扣款/入账前后终止并重启 | 单元状态可由资产指令确定性恢复，无重复扣款、重复入账或跨单元步骤穿透 | P1-007 |
| PHY-007 | 尝试修改/删除交割单元经济字段或事件 | 数据库拒绝；只允许受控状态迁移和追加事件 | P1-007 |
| PHY-008 | 退款、拍卖、平台垫资或自动放弃未获批准 | 系统没有隐式路径执行这些动作；实物合约上市门禁保持关闭 | P1-007 |
| OPS-001 | 构造失败/人工资产指令、开放对账、待复核结算价、过期风险账户及各类积压 | 工作台数量、最早时间和分页明细与数据库精确一致，租户之间不可见 | P1-008 |
| OPS-002 | 从工作台重试普通异常指令 | 只有失败/人工态可恢复，原指令号和经济字段不变；非异常态返回不允许操作 | P1-008 |
| OPS-003 | 通过通用或结算指令入口重试实物交割指令 | 两个入口均拒绝；只有带理由的交割单元恢复入口可执行 | P1-007/P1-008 |
| OPS-004 | 保险流水、平台兜底和未解决穿仓包含多个币种；保险入金、赔付、人工提取分别造数 | 原始流水代数和、兜底负债和缺口分币种精确；明确标注非 Asset 余额；赔付/提取按出金负号落账；日终逐笔及余额与 Asset 核对差额为 0 | P1-008 |
| OPS-005 | 告警首次异常、持续异常、连续三个正常窗口及通知失败 | 级别、保护、通知对象、案件和恢复关闭证据符合告警目录；通知失败另行升级 | P1-008 |
| OPS-006 | 独立Option工作进程claim指令，真实Asset按原业务号成功冻结并落流水；代理阻断响应后对工作进程发送`SIGKILL`，不删除其Redis租约；租约自然到期后同时启动两个全新工作进程竞争接管 | 强杀点指令保持`PROCESSING/retry=0`且Asset冻结/流水各1；租约有效期内新实例拒绝，30秒后自然释放；两个接管实例只有一个进入Asset并沿用原指令/业务号，另一个收到任务已运行；最终`SUCCESS/MATCHED/retry=0`，冻结/流水仍各1、钱包镜像一致、无任务锁残留。仓库门禁可在真实租约到期后加速独立的60秒DB超龄时间；预生产必须按真实墙钟和容器编排执行 | P1-008 |
| OPS-007 | 制造资产/对账/风险/事件/行权/结算/强平/实物/穿仓异常，并模拟采样失败、恢复和标签消失 | `:9105/metrics` 按租户输出精确数量、最早时间和分币种金额；15秒内不重复查询；失败显式告警且保留最后成功序列；恢复把旧序列置0；健康租户无非0泄漏；规则可触发并按连续窗口恢复 | P1-008 |
| OPS-008 | 制造缺配置拒单、价格带拒单率恰好10%/超过10%/1分钟21笔、组合腿控制拒绝回滚、5分钟51笔 STP、21笔限额拒单、熔断/kill switch 30秒残单、异常成交更正和 MMP 残单/错误/异常禁用；另建健康租户 | 10%边界不触发，超过10%或20笔触发；每次控制评估均有分母且组合回滚不丢审计；每个异常只在目标租户输出精确对象数/最早时间；健康租户无非0；OPT-A017～A024规则存在；查询命中监控索引；迁移双执行 | P1-002/P1-003/P1-008 |
| OPS-009 | 交易态合约分别构造标的陈旧、标记价陈旧、Greeks 阈值0/缺失/边界/过期/未来及健康租户；风险扫描构造20组中2组失败、下一次0组和收集阶段失败 | 标的/标记价按30秒精确计数且租户隔离；Greeks 按逐合约阈值，等于边界不报警、超一秒报警，0 fail closed；风险总数20、失败2、比例0.1，下一次旧序列显式清零 | P0-003/P1-001/P1-008 |
| OPS-010 | 在异常租户构造到期无结算价、已确认结算证据不符、组合持仓无参数、风险快照版本落后、生效未完成/失败公司行动、24小时系列积压、生成系列缺谱系、交易链 Call/Put 缺腿和 HOLDING 多空不平衡；另建完整健康租户 | 10个固定治理类别各只输出目标租户精确数量1及约定最早时间；健康租户无非0泄漏；OPT-A010/A011/A025/A027～A029 的10条分级规则存在；17项监控索引可重复迁移且关键查询命中 | P0-005/P1-004/P1-008/P2-004/P2-005/P2-006 |
| OPS-011 | 管理员分别尝试修改已上市合约状态、经济字段和系列合约经济字段，并构造5分钟内4次重复；再传入未知对象/原因测试标签边界 | 请求均拒绝且数据不变；Counter 只产生 tenant_scope/contract/三个固定原因或 other；任意一次触发 SEV-3，超过3次触发 SEV-2；结构化日志保留租户、合约、管理员和原因；直接 SQL 尝试由数据库审计单独验收 | P1-008 |
| OPS-012 | Asset/财务日终结果写入一条 `check_type=3,status=OPEN` 差额，另建无差额健康租户；随后以同 issue_key 重跑并恢复 | 异常租户 `daily_conservation_issue=1` 且最早时间精确，健康租户无非0；规则立即触发 SEV-1；重跑不换键并累计次数；差额为0后原问题保留且转已恢复；scope=2 零差额成功心跳缺失会报警 | P1-008 |
| OPS-013 | 在同一测试库构造健康钱包、金额不一致、Option/Asset 单边缺失、锁定余额、遗留非0 `account_id` 和18位小数，连续执行日终钱包镜像任务 | 健康与 `0.000000000000000001` 租户 scope=1 attempt 成功；差异逐币建立 `ACCOUNT_MIRROR` 案件；同日重跑 attempt 递增不覆盖；修复后案件恢复；运行行 UPDATE/DELETE 被拒绝；最近差异/失败及36小时无成功触发 OPT-A031 | P1-008 |
| OPS-014 | 构造健康用户/平台流水、UTC截止后流水、无当日流水、18位小数、未知分类、流水链断裂、余额恒等式错误、Option指令单边缺失/重复关联和跨租户明细 | 健康范围逐币scope=2零差额成功；截止后流水不污染前一日；所有不完整场景均为INCOMPLETE且无成功心跳；最小单位差异精确；attempt递增；运行/明细改删、错误公式和跨租户挂接均被数据库拒绝 | P1-008 |
| OPS-015 | 分别构造 outbox/inbox 持续增长、到期前30分钟待处理行权、启用 kill switch 后控制撤单释放指令失败、未逾期补资单元及逾期/违约单元；另建健康租户 | 增长保持3分钟触发 A008；近到期行权和释放失败只在异常租户输出精确数量；普通实物异常为 SEV-2，逾期/违约另触发 SEV-1；既有迁移校验和不变，3项增量索引迁移双执行且新查询命中 | P1-007/P1-008 |
| CAL-001 | 纽约时区普通交易日、DST 春季前后和秋季前后相同本地会话 | 均按 IANA 时区对应正确 UTC 窗口；不存在固定偏移一小时错误 | P2-001 |
| CAL-002 | 同日两段会话、跨午夜会话及开/闭边界前后一秒 | 每段开时刻允许、闭时刻拒绝；跨午夜归属原会话日且无空档穿透 | P2-001 |
| CAL-003 | 周会话开放，但命中节假日 `CLOSED`；常规闭市但命中补班 `OPEN` | `CLOSED` 拒绝，补班允许；同时命中时 `CLOSED` 优先 | P2-001 |
| CAL-004 | 日历草案含重叠会话、非法时区、零长度窗口、重叠有效版本或创建人自审 | 全部拒绝，不改变当前有效版本和合约准入 | P2-001 |
| CAL-005 | 未来版本在生效前一秒、时刻和后一秒解析 | 生效前使用旧版本；生效时唯一切换新版本；下单和运营查询版本一致 | P2-001 |
| CAL-006 | 合约缺少日历、日历未批准、已过期或数据库存在歧义 | 下单 fail closed，不冻结、不入簿并产生可定位审计/水位 | P2-001 |
| CAL-007 | 活动订单与并发新单存在时触发临时休市 | halt 返回前新单均拒绝或已被事务序列化；活动单全部幂等撤销并释放资金 | P2-001 |
| CAL-008 | 重复休市、错误 halt 恢复、休市中熔断、带理由正确恢复 | 重复操作不重复资金动作；错误引用和熔断绕过被拒绝；正确恢复留完整操作链 | P2-001 |
| CAL-009 | 交易闭市期间运行风险扫描、强平、主动行权、到期和结算 | 非交易任务继续执行，不能因闭市跳过风险或清算义务 | P2-001 |
| ACT-001 | 同一公司行动公告重复、乱序到达并随后发布修订版 | 原版本不可覆盖；只生成一个当前版本；暂停和后续计划不重复 | P2-004 |
| ACT-002 | 创建人尝试自审，或缺少官方来源、证据哈希、关键日期和精确转换参数 | 审批拒绝，合约/持仓无变化 | P2-004 |
| ACT-003 | 活动订单、未完成 outbox/inbox、资金指令、行权、强平或结算任一存在 | 执行 fail closed；旧合约保持暂停，无部分迁移 | P2-004 |
| ACT-004 | 支持的单一后继现金合约比例调整 | 新旧数量、OI、成本基础、收益分项和风险敞口按方案精确守恒 | P2-004 |
| ACT-005 | 空头存在多笔 margin lot 和释放/消费中的担保 | 未完成批次不执行；完成后每笔剩余数量/保证金/冻结身份一一可追溯 | P2-004 |
| ACT-006 | 5001 个多空持仓迁移并在每 100 条边界和提交前后终止任务 | 原批次幂等续跑，无漏项、重复项、分页截断或双持仓 | P2-004 |
| ACT-007 | 转换产生无法按数量步长或 `DECIMAL(32,16)` 表达的余数 | 不静默截断；仅按获批现金差额/零碎规则处理，否则人工阻断 | P2-004 |
| ACT-008 | 合并篮子、代币分叉/空投、实物交割物变化或未知下架规则 | V1 自动执行拒绝；合约保持暂停并建立人工案件 | P2-004 |
| ACT-009 | 执行后尝试修改/删除事件、方案、迁移明细或原持仓历史 | 数据库拒绝；修正只能创建补偿方案并重新审批 | P2-004 |
| ACT-010 | 调整前后运行风险、到期、行权和 Asset 对账 | 只有切换完成的后继合约参与交易；义务不中断，逐币差额为 0 | P2-004 |
| SER-001 | 相同请求键和完全相同输入重复创建，包含首次响应丢失后重试 | 只存在一个系列版本，返回相同 ID、payload hash 和规格 | P2-005 |
| SER-002 | 相同请求键提交不同参考价、模板、expiry 或 strike band | 明确拒绝，不返回旧请求成功假象，不创建新版本 | P2-005 |
| SER-003 | 创建人自审、两名复核人并发批准或批准响应丢失后重试 | 自审拒绝；并发只有一个事务生成；重试返回同一批合约 | P2-005 |
| SER-004 | 一个生成代码与现有合约碰撞，或任一生成合约不通过现有校验器 | 整批回滚，系列仍待复核，0 条合约和0条谱系明细 | P2-005 |
| SER-005 | 多 expiry、多 band，检查每个 expiry/strike 的 Call/Put | 价格集合完全相同且一一成对；无重复、遗漏和 band 边界双计 | P2-005 |
| SER-006 | band 重叠、上界不可由步长精确到达、超过16位小数或整数位溢出 | 写库前拒绝，不截断、不使用二进制浮点近似 | P2-005 |
| SER-007 | 500 条和 501/502 条生成边界 | 500 条原子成功；超限在写库前拒绝 | P2-005 |
| SER-008 | 修改/删除已提交规则、expiry、band 或生成谱系，或批准旧版本 | 数据库/服务拒绝；修订只创建更高版本 | P2-005 |
| SER-009 | 生成后未做上市复核、创建人自批上市、批准后回退，或上市时日历/行情缺失陈旧 | 前三者拒绝并保持 PENDING；仅独立上市复核批准且运行时日历有效、标的/标记价30秒内新鲜才可推进 | P2-005 |
| SER-010 | 用目标市场年度节假日、DST、周/月/季到期样本核对显式 UTC 时间 | 与独立复核清单完全一致；系统不自行推算顺延规则 | P2-005 |
| DB-001 | 全量迁移连续执行两次 | 结构、索引、CHECK、触发器和历史回填一致且无失败 | 通用 |
| DB-002 | 原地修改已发布结算价或已提交行权指令 | 数据库拒绝；新增版本和指令状态迁移成功 | P0-005/P1-006 |
| REC-001 | outbox/inbox/资金指令重复投递 | 状态及资金结果只生效一次 | 通用 |
| REC-002 | 任务处理中进程终止后恢复 | 原业务号继续，无重复资金动作 | 通用 |

## 4. 会计恒等式

每笔交易/行权/结算至少验证：

```text
买方权利金扣款 = 卖方权利金入账 + 交易手续费
空方行权/结算扣款 = 多方净入账 + 行权手续费
保证金初始冻结 = 已消耗 + 已释放 + 当前剩余 + 当前待处理
用户总收益 = 权利金交易盈亏 + 行权/结算盈亏 - 各类费用
```

系统级验证：

```text
所有用户 Asset 净变化
+ 费用账户净变化
+ 保险账户净变化
+ 平台兜底资产变化
= 0
```

若产品存在外部注资或交割资产转移，必须以独立业务号作为等式中的明确外部项。

## 5. 故障注入

- Asset 超时、成功后响应丢失、重复响应、余额不足。
- 行情消息乱序、重复、断流、未来时间戳和窗口样本不足。
- MySQL 死锁、事务回滚、任务进程在每个资金步骤前后终止。
- Redis 锁超时和重复调度。
- 501 个以上指派候选与并发平仓/撤单。

任何故障都不得通过修改业务号来“修复”；恢复必须证明幂等重放。

## 6. 测试记录

| 用例 | 版本/提交 | 环境 | 结果 | 资金差额 | 缺陷/证据 | 执行人 | 复核人 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| PHY-001、PHY-007、DB-001 | 当前工作区 | 临时 MySQL 8.4（tmpfs）+ Go 单测 | 仓库级通过；预生产未执行 | 不适用 | 迁移双执行；FIFO 配对；经济字段修改、非法状态和删除均被拒绝 | Codex | 待复核 |
| PHY-002 屏障部分 | 当前工作区 | 临时 MySQL 8.4（tmpfs） | 仓库级通过；Asset E2E 未执行 | 不适用 | A 单元 step 1 失败时 B 单元 step 3 可运行；传统 `biz_no` step 2 仍被阻止 | Codex | 待复核 |
| OPS-001、OPS-004、DB-001 | 当前工作区 | 临时 MySQL 8.4（tmpfs）+ Go 集成测试 | 仓库级部分通过；预生产未执行 | Option 原始流水聚合断言通过；Asset 差额待 E2E | 跨模块计数/最早时间 SQL 全部执行；USDT 人工注资流水 7、兜底 3、未解决缺口 2 断言；该造数不覆盖赔付负号，赔付符号修复及历史迁移待财务/清算批准；权限迁移双执行后 6 条唯一记录 | Codex | 待复核 |
| OPS-007（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + Go 单测/集成测试 + YAML 校验 | 仓库级通过；Prometheus/Alertmanager 预生产演练未执行 | USDT 原始保险流水代数和7已断言；Asset 实时差额待 E2E | 异常租户资产待处理1/最早800、事件积压2/最早800、组合超龄2/最早800、组合人工1/最早850、结构异常3、缺腿成交组1；健康租户无非0水位；6项通用指标注册、15秒节流、失败保留和恢复清零通过 | Codex | 待复核 |
| OPS-008、DB-001（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + Go 单测/集成测试 + YAML/EXPLAIN | 仓库级通过；通知/连续恢复预生产演练未执行 | 不适用 | 缺配置、熔断、更正、STP、限额、暂停/kill switch残单及MMP水位断言通过；价格带10%边界不触发、2/10和21/21精确触发，成功/拒绝均写分母且组合回滚重记审计；健康租户无非0；比例查询两个分支命中`idx_option_control_event_monitor` | Codex | 待复核 |
| OPS-009（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + Go 单测/集成测试 + YAML + Admin UI类型检查 | 仓库级通过；风险故障注入/通知预生产演练未执行 | 不适用 | 标的/标记30秒断言；Greeks 阈值边界健康、超一秒/0/未来/缺行异常且租户隔离；迁移双执行和数据库交易态门禁；后台显示逐合约阈值与状态 | Codex | 待复核 |
| OPS-010（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + Go 集成测试 + YAML/EXPLAIN | 仓库级通过；外部探针和生产通知未执行 | 不适用 | 异常租户结算逾期/证据异常、组合缺配置/版本落后、公司行动到期/异常、系列积压/谱系异常、公开链缺腿/OI不平衡各1；健康租户无非0；当前通用规则51条无重名；17项Option监控索引创建路径、双执行及关键 EXPLAIN 命中 | Codex | 待复核 |
| OPS-011（仓库部分） | 当前工作区 | Go 单测 + YAML | 仓库级通过；数据库审计和通知未执行 | 不适用 | 已上市状态/经济字段及系列经济字段三个拒绝分支记录固定标签 Counter 和结构化日志；未知标签归一为 other；两级规则存在；当前通用规则合计51条 | Codex | 待复核 |
| OPS-012（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + Go 集成测试 + YAML | 仓库级通过；scope=1钱包镜像及scope=2完整资金守恒生产者/心跳已实现，预生产真实调度与通知待执行 | Option 差异对象、钱包镜像及完整期初/期末、流水、平台账户和子账口径已断言 | `check_type=3` 开放差额只在异常租户输出1/最早99950；scope=1健康/差异/单边缺失/遗留账户精确断言；scope=2成功/差异/恢复闭环和问题单恢复通过；运行/明细不可改删；OPT-A015/A031规则存在；当前通用规则合计51条 | Codex | 待复核 |
| OPS-013（仓库部分） | 当前工作区 | Go 单测 + 隔离 MySQL 8.4 + YAML | 仓库级通过；生产调度/通知待执行 | 不适用 | 租户9：BTC 2/2、ETH 1/1、USDT 2/1 钱包差异；租户10：USDT 1/0；租户11：ETH `1e-18` 精确一致；成功/差异/缺失指标精确，attempt UPDATE/DELETE 被触发器拒绝，精度扩列及运行迁移连续执行两次 | Codex | 待复核 |
| OPS-014（仓库部分） | 当前工作区 | Go 单测/竞态/vet + 隔离 MySQL 8.4真实事务 + EXPLAIN + goctl生成 | 仓库级通过；预生产真实流水、调度、容量和通知待执行 | 用户/平台金额差额及Option子账Asset-bill差额已断言 | 复算模型全部场景通过；自动任务在REPEATABLE READ事务内追加运行及3类不可变明细；真实闭环attempt状态为成功/差异/成功，差异钱包精确为1，稳定问题单自动恢复；明细UPDATE被拒绝；两项Asset索引命中；50张表/50个生成模型一致 | Codex | 待复核 |
| OPS-015（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + Go 集成测试 + YAML/EXPLAIN | 仓库级通过；Prometheus 连续窗口和通知未执行 | 不适用 | 异常租户近到期行权1/99900、kill switch 释放失败1/99920、实物异常2/99800、逾期/违约1/99900；健康租户四类均无非0；A008/A009/A020/A026 四条补充规则存在；`zr` 校验和保持不变，`zs` 增量迁移双执行且3项新索引命中 | Codex | 待复核 |
| PM-010（仓库部分）、DB-001 | 当前工作区 | 隔离 MySQL 8.4 + Go 集成测试 + 生成/编译/前端构建 | 仓库级通过；预生产真实版本切换与风险扫描对照未执行 | 不适用 | `zv` 迁移连续执行两次；新组合卖单缺省、错版本、失效边界、不成对和非适用引用均拒绝；正确 ID/版本落单后不可改，正常状态更新成功；迁移前 `0/0` 历史单正常撤单；与 MMP 多触发器并存复测；`make gen-model`、Option test/vet/race、Admin API test/vet、Admin UI type-check/build 和 repository readiness 通过 | Codex | 待复核 |
| ASN-001、ASN-003、ASN-004、EX-001、EX-002、PNL-001（仓库与真实RPC部分）、DB-001 | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + Go 单元/集成测试 + EXPLAIN | 仓库级通过；501/5000真实Asset、并发平仓竞争和多实例强杀未执行 | 空头扣120=多头净入108+手续费12；开仓权利金30后多头/空头A/空头B收益`78/-30/-60`，加平台费用12合计0；四方总资产400，冻结0 | FIFO 查询按 `(create_times,id)` 每页500；1/500/501/5000无截断/重复/乱序且索引命中。真实RPC同键20并发只生成1笔/一次冻结，同键不同数量拒绝，两个并发清算只形成2个FIFO指派、6指令/6流水且重放无新增；两个空头分别全量指派1/2张，所有仓位与margin lot结清 | Codex | 待复核 |
| PNL-001、PNL-002（部分平仓部分）、ASSET-001、ASSET-002 | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + 真实撮合/持仓事件 | 仓库级通过；并发平仓未执行 | 成交额15，买/卖费用`0.6/0.3`；平仓方钱包93.7，新买方84.4，原空头120，平台1.9，总资产300/冻结0 | 真实撮合1张部分平仓；买方扣冻15.6=卖方净入14.7+平台费用0.9，3指令/3流水成功对账；平仓方剩1张，`trade_realized=5`、`unrealized=5`、`fee_paid=1.3`、`total_return=realized_pnl=3.7`；新买方`total_return=-0.6`；重跑无新增 | Codex | 待复核 |
| ORD-001、ACCT-001、EQ-001、PNL-002、ASSET-001、ASSET-002（隔离 Call LIMIT 开仓） | 当前工作区 | 开放交易日历 + 公开 `PlaceOrder` + 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + 撮合/资金/持仓/风险任务 | 仓库级通过；并发多实例及同仓位后续生命周期未执行 | 卖方100冻结逐仓保证金50；买方100冻结权利金及费用10.4；成交后买方89.6、卖方109.8（可用59.8/冻结50）、平台0.6，总钱包200 | 2订单均FILLED、2客户端键、1成交/成交额10，买/卖费用0.4/0.2；5指令/5流水成功对账，1个outbox/1个inbox完成，2持仓、1个50保证金lot、2个钱包级NORMAL风险账户；买/卖总收益-0.4/-0.2；重放买方客户端订单号仍返回原订单且全部计数不变 | Codex | 待复核 |
| ORD-002、ORD-003、OPS-003、ASSET-001、ASSET-002（用户/管理撤单、IOC/FOK、冻结竞态） | 当前工作区 | 公开 `PlaceOrder/CancelOrder` + 管理批量强撤 + 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + 并发事务 | 仓库级通过；多实例强杀、其他币种管理强撤和消息任务接管未执行 | 冻结前撤单不调用Asset；冻结后撤单全部释放。管理员对已冻结/待冻结各一单强撤后，两钱包各100/冻结0。20轮撤单/冻结竞态每轮钱包恢复100/冻结0。IOC部分成交和FOK零成交继续守恒 | 管理强撤跨租户拒绝；授权操作人9003产生2条不可变逐单审计，3指令为2成功对账+1冻结前取消，重放不新增且审计改删被拒。竞态每轮同时1个资金任务+2个撤单请求，恰好1成功/1业务拒绝，20订单/20客户端键均CANCELED，指令号无重复，稳定不变量`canceled + success/2 = 20`；用户撤单/IOC/FOK既有证据继续通过 | Codex | 待复核 |
| ORD-004、ORD-005、ASSET-001、ASSET-002（MARKET、POST_ONLY） | 当前工作区 | 公开 `PlaceOrder/CancelOrder` + 保护价/最大成交额 + 隔离 MySQL 8.4 + 真实 Asset gRPC + 撮合/资金/持仓/风险任务 | 仓库级通过；盘口多档扫单、保护价越界并发、MMP真实流量及其他币种未执行 | MARKET买方预留12，成交价10实际消费10.4并释放1.6；三方钱包`89.6+109.8+0.6=200`、卖方冻结50。POST_ONLY会吃单时零成交撤销，不会吃单的8价委托入簿后公开撤销；三名用户各恢复100/冻结0 | MARKET 2单FILLED、1成交，6指令/6流水成功对账、2持仓/1个50 lot/outbox/inbox各1；POST_ONLY 3单最终CANCELED、0成交/0持仓，6指令/6流水成功对账，`POST_ONLY_WOULD_TAKE`和入簿后`USER_CANCEL`证据各1；重放无新增 | Codex | 待复核 |
| LIQ-001、LIQ-002、LIQ-003、PNL-002（强平部分）、ASSET-001、ASSET-002 | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + 风险/强平/资金任务 | 无穿仓及穿仓逐仓整仓、逐仓自动部分数量、账户级组合整仓顺序清算和恢复后取消通过；组合部分数量、跨币种折算和生产容器编排未执行 | 无穿仓：原空头保证金扣88/释放12，保险账户入账80并冻结80，平台8；逐仓部分：120本金下2张自动选1张，源仓保留1张/保证金50，接管1张/保证金40；穿仓：担保50+保险15+平台23=接管80+费用8；组合：500抵押下两个空头逐笔接管并保护剩余初始保证金 | 无穿仓5指令、部分数量5指令、穿仓4指令均成功对账；部分数量资金前仓位2/可用1/冻结1、lot数量1/保证金100/pending50，完成后源/接管维持保证金28/28、三方钱包120/冻结90，重放无新增；保险接管库存不递归强平。组合8条清算+2条余量释放成功，补资恢复CANCELED/retry0/0指令，状态7约束及迁移双执行通过 | Codex | 待复核 |
| COIN-001、COIN-002、COIN-003（仓库部分）、DB-001 | 当前工作区 | 隔离 MySQL 8.4（库/连接使用不同 collation）+ 独立 Redis + 真实 Asset gRPC + Go 单元/集成测试 + YAML | 仓库级通过；现金USDT用户撤单/IOC/FOK已覆盖，实物币种用户撤单/IOC/FOK及到期/管理强撤/强平全路径仍待预生产 | 冻结0.25 BTC/300 USDT并分别全额释放；现金USDT冻结前/后撤单、IOC撤余和FOK不足均精确归零 | 历史实物 Call 回填 BTC、Put/买单/现金单回填 USDT；错误币种/空币种及证据改写均拒绝；真实控制撤单精确生成 BTC/USDT 两条释放指令并成功对账；现金Call新增用户撤单、IOC部分成交撤余和FOK不足全额释放，订单终态、钱包/镜像和重跑幂等通过；异常租户 `margin_coin_invalid=3/oldest=900`；OPT-A032存在 | Codex | 待复核 |
| ACCT-001、EQ-001、EQ-002、PNL-001/002、LIQ-001/002/003、ASSET-001、ASSET-002（真实RPC仓库验收） | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + Option 任务 | 通过；隔离 Call五类普通单、用户/管理撤单、逐仓自动部分强平、账户级组合清算、单实例资金竞态及独立进程SIGKILL/双实例接管已覆盖；全币种和其余预生产故障注入仍未执行 | 基础冻结释放差额0；普通单/撤单、部分平仓、主动行权、到期、逐仓及组合强平的权利金、payoff、费用、用户钱包、平台账户与持仓收益守恒；所有场景无重复流水 | 钱包风险账户均`account_id=0`；管理强撤2成功+1合法取消及2条不可变审计；20轮竞态每轮只允许1取消或2成功；逐仓部分强平自动选1/2张且5指令对账；组合清算8条清算和2条余量释放成功并保护剩余初始保证金，补资恢复旧记录CANCELED且0指令。全套固定门禁为`success+2*canceled=134`，最近132条中130成功/2取消；SIGKILL接管冻结/流水唯一；迁移双执行后唯一历史冻结补账1、重复键补账0；命令`services/option/acceptance/run-p0-asset-rpc-e2e.sh` | Codex | 待复核 |
| EX-004、EX-005、EX-008、SET-002、PNL-001（到期部分）、DB-002（真实RPC仓库验收） | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + Option 生命周期任务 + 双执行治理迁移 | 仓库级通过；EX-007真实状态切换/毫秒截止跨界、大批量与生产调度/通知待执行 | 空头扣20=自动行权多头净入18+手续费2；开仓权利金后AUTO/DNE/空头收益`8/-10/0`，加平台费用2合计0；最终钱包`108/90/200/2`，四方总资产400 | 最新AUTO持仓行权1张、DNE持仓0张，空头结算明细只指派1张；DNE在到期阶段钱包不再变动；4指令/4流水全部成功对账，批次credit/debit=20/20，重跑不新增；TRADING/PAUSED状态规则有单元枚举证据；经济字段改写、SUPERSEDED→ACTIVE及DELETE均被数据库拒绝 | Codex | 待复核 |
| PRICE-001～PRICE-008（仓库部分）、DB-001 | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + Go 单元/集成测试 + SQL/EXPLAIN | 仓库级现金到期通过；生产调度/通知、费用/部分数量/多账户、大批量及其余故障注入未执行 | 卖方冻结50后扣20、释放30，买方入账20；最终120+80=200、冻结0 | `zy`迁移连续执行两次；自动价按不可变快照复算；错误中位数/证据/审批/改删均拒绝。真实现金 Call 使用119/120/121中位数120，错误确认价121被拒，无价合约结算数0；3条结算指令成功且对账，结算状态3、批次7、合约5，重放无重复；最终`make gen-model`已在本轮DDL定稿后通过 | Codex | 待复核 |
| SET-001（依赖不可用/成功响应丢失） | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 两种一次性故障模式 + 真实 Asset gRPC + Option任务 | 仓库级通过；真实容器强杀和生产调度仍未执行 | 扣款前失败时0条结算流水；扣款后丢响应时已有唯一扣款流水1；两者最终均扣20/入20/释放30、120+80=200、冻结0 | 两场景指令均FAILED/retry=1且step2成功/流水0；前者卖方50仍冻结，后者真实Asset已扣20。仅推进重试时间并沿用原instruction_no 后，前者新建唯一扣款流水，后者幂等认领原流水；每场景三指令成功/对账、max_retry=1、总流水3，重复任务不增加 | Codex | 待复核 |
| SET-003（余额不足/补资/人工恢复） | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + Option任务 + 不可变审计触发器 | 仓库级通过；预生产财务审批、用户通知和真实调度仍未执行 | 初始双方200，外部补资20；最终买方220、空方0、合计220、冻结0；结算三流水及补资流水各唯一 | 卖方先扣冻结50，再因可用余额50不足以扣70连续失败20次，step2始终无成功/流水；第20次进入MANUAL_REVIEW。缺原因、65字符超长原因和缺操作人的重试均拒绝且保留retry_count=20；独立业务号补资20后以`SETTLEMENT_BALANCE_TOPUP_VERIFIED`、operator 9002恢复，原指令唯一扣70。唯一审计事件保存fromStatus=5/fromRetryCount=20/原错误，UPDATE/DELETE均被触发器拒绝；3指令最终成功并对账 | Codex | 待复核 |
| OPS-006（超龄处理中恢复） | 当前工作区 | 隔离 MySQL 8.4 + 独立 Redis + 真实 Asset gRPC + 3个独立Option工作进程 + SIGKILL | 仓库级真实进程通过；预生产容器编排、真实60秒墙钟、调度和告警通知仍未执行 | 既有结算恢复最终扣20/入20/释放30、总额200；新增强杀冻结场景最终钱包100/可用90/冻结10，资产变化仍只有10 | 第一进程在真实Asset冻结成功后、Option收响应前被SIGKILL，数据库为PROCESSING/retry0且冻结/流水各1；30秒Redis租约自然到期前新实例拒绝。到期后两个新进程竞争，恰好一个进入Asset并认领原流水；最终SUCCESS/MATCHED/retry0、冻结/流水各1、锁清理。仅DB超龄年龄在租约到期后加速至61秒 | Codex | 待复核 |
| CAL-001、CAL-002、CAL-003、CAL-004（算法部分）、DB-001 | 当前工作区 | 临时 MySQL 8.4（tmpfs）+ Go 单测 | 仓库级部分通过；预生产未执行 | 不适用 | 纽约 DST、开闭边界、跨午夜、24×7、例外优先级、重叠及非法时区断言通过；P2-001 迁移连续执行两次通过，未来版本切换仍待专项验证 | Codex | 待复核 |
| ACT-002、ACT-004、ACT-007、ACT-008、ACT-009（代码/数据库部分）、DB-001 | 当前工作区 | 临时 MySQL 8.4（tmpfs）+ Go 单测/编译/类型检查 | 仓库级部分通过；预生产未执行 | 自动路径现金差额固定为 0 | 整数比例、16 位精度、后继数量步长和成本基础守恒通过；P2-004 迁移双执行、证据/状态/明细不可篡改、margin lot 原始身份保持、4 菜单/1 任务唯一性通过；Option 全量 Go/race/vet、Admin API 与 Admin UI 检查通过 | Codex | 待复核 |
| SER-001、SER-003～SER-009（仓库/数据库部分）、DB-001 | 当前工作区 | 临时 MySQL 8.4（tmpfs）+ Go 单测/编译/类型检查 | 仓库级部分通过；并发服务 E2E 未执行 | 不适用 | 精确梯度、Call/Put 对称、500/超限、强制 PENDING、确定性代码和上市行情门禁单测通过；迁移双执行、生成/上市同人审批拒绝、合法独立批准、批准回退拒绝、证据/谱系不可变、冲突事务0残留及5权限唯一性通过 | Codex | 待复核 |
| COMBO-009（仓库部分）、DB-001 | 当前工作区 | Go 单测/编译/类型检查 + 隔离 MySQL 8.4 临时库 | 仓库级通过；权限与真实 Asset 强撤演练未执行 | 不适用 | 管理筛选租户条件、父单关联成交/指令 SQL、BFF 深层 DTO 映射、Option/Admin API 全量测试/vet 和 Admin UI 类型检查通过；权限迁移连续两次后3条唯一记录且父权限均为729 | Codex | 待复核 |
| COMBO-010（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + SQL 租户作用域单测 + Go/前端类型检查 | 仓库级通过；预生产外部告警未接入 | 不适用 | 异常租户精确返回超龄2、人工态1、最早800、结构异常3、缺腿成交组1；健康租户五项均为0；阈值10/300边界、运营概览/BFF/工作台字段贯通，Option/Admin API 全量 test+vet、组合 race、Admin UI 类型检查通过 | Codex | 待复核 |
| COMBO-011（仓库部分） | 当前工作区 | 隔离 MySQL 8.4 + Go/SQL 单测 + YAML/Compose 校验 | 仓库级通过；Prometheus/Alertmanager 预生产演练未执行 | 不适用 | 完整两腿且全扣款成功放行；重复腿号、缺扣款拒绝；异常租户超龄屏障2、健康租户0；普通盘口和matcher失败关闭、事务二次屏障、4项低基数指标、9105内部端口及OPT-A030规则已验证 | Codex | 待复核 |
|  |  |  | 通过/失败/不适用 |  |  |  |  |
