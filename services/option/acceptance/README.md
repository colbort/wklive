# Option P0 Asset RPC 验收

`run-p0-asset-rpc-e2e.sh` 是仓库级真实 RPC 门禁，不用 mock Asset。脚本在隔离 MySQL 数据库和
独立 Redis 中启动当前工作区的 Asset gRPC，再直接运行 Option 风险扫描及资产指令任务。

## 执行

前置条件：`deploy/docker-compose.yml` 中 MySQL、Etcd 已运行，本机可使用 Docker、Go 和 `nc`。

```sh
services/option/acceptance/run-p0-asset-rpc-e2e.sh
```

脚本只会删除名称匹配 `wklive_option_p0_asset_e2e*` 的隔离数据库、带唯一进程号的临时 Redis
容器和本次创建的 Etcd 键；默认结束即清理。设置 `OPTION_P0_E2E_KEEP=1` 可保留数据库用于
人工取证，但 Redis、Etcd 配置和 Asset 进程仍会关闭。

### Beanstalkd 原生架构门禁

`deploy/docker-compose.yml` 已不再使用仅有 AMD64 的 `schickling/beanstalkd:latest`。两个实例现在由
仓库内 `Dockerfile.beanstalkd` 从固定 digest 的 Alpine 3.20 多架构清单构建，Beanstalkd 固定为
`1.13-r0`，且 Compose 不强制 `platform`；Apple Silicon 会生成原生 `linux/arm64` 镜像，AMD64
Linux 会生成原生 `linux/amd64` 镜像。协议健康与宿主/镜像架构一致性由
`deploy/beanstalk-readiness.sh` 检查，并接入 Option production readiness。两个实例使用独立 WAL
volume和`-f 0`同步写盘；`deploy/beanstalk-restart-smoke.sh`以隔离临时容器证明整容器重建后任务仍可取回。
架构及持久化门禁通过不等于
消息容量已经签署；预生产仍须重跑积压、重连、强杀恢复和容量验收。

## 当前覆盖

- ACCT-001：同一用户跨两个业务 `account_id` 只形成 `account_id=0` 钱包风险账户。
- RISK-001/RISK-002：租户996041同时构造一个120秒陈旧行情钱包、一个单用户 Asset
  `Unavailable`钱包和一个健康钱包。任务在聚合返回两个失败的同时，把前两者写为
  `RESTRICTED/last_calc_time=0`，健康钱包同周期写为`NORMAL`。同一次生产语义的`tenant_id=0`全局扫描
  还包含租户996042的健康钱包，并在故障聚合返回前把它刷新为`NORMAL`，证明单钱包错误不终止后续租户。
  指标发布按作用域处理：全局扫描清零已消失租户，单租户重放只更新目标租户；重置非活动账户失败也不
  推进最后完成时间。
- PM-003/PM-004/PM-010：租户996051通过管理逻辑创建并由独立管理员批准V1、未来V2及复制V1
  参数的回滚V3。三个生效阶段各由公开`PlaceOrder`提交一张组合卖单，并运行真实Asset余额参与的
  风险扫描；订单和扫描依次使用V1/V2/V3，最终三个钱包均保存V3。正式证据为4条版本记录
  （1批准/2替代/1拒绝）、1条`source_config_id`回滚来源参数全等、2个旧版本有效期精确闭合、
  3个不同订单版本。服务及MySQL触发器共同拒绝追溯草案、到期后审批、伪造来源及改写来源。
  该场景同时覆盖首次组合准入创建风险行后的唯一键负缓存失效，防止紧随其后的扫描重复插入。
  `zh`、`zv`及`20260802_option_portfolio_risk_config_lineage.sql`均连续执行两遍；本项有DDL，
  `make gen-model`已执行并同步共享proto。
- SER-001～SER-009：20路相同 request key 并发创建只形成1个系列；两名生成复核人并发和成功
  响应丢失重放只形成1批500个 `PENDING` 合约（2 expiry、125 strike、250 Call/250 Put）。
  单代码碰撞整批回滚，502输入在写库前拒绝。创建人自批生成/上市均拒绝；独立上市复核后，缺行情、
  31秒陈旧行情和错误 due identity 仍保持 `PENDING`，仅完整运行时门禁允许目标合约上市。系列 schema
  和生成合约经济字段保护迁移均连续执行两次；本轮触发器 DDL 后已执行 `make gen-model` 且无字段差异。
- CHAIN-001～CHAIN-006：公开链使用显式只读 `REPEATABLE READ`；精确到期、默认/暂停状态、
  Call/Put缺腿、24h双闭区间和OI平衡质量均由真实MySQL验证。500合约形成250个完整行权价，
  第501条使请求整体拒绝；100×2档盘口只聚合201笔合法`LIMIT/POST_ONLY`活动普通单，101档拒绝。
  24个读取者共144次与撤单/成交写事务并发时只看到完整前态或后态、撕裂0；16路最大请求以及
  跨租户行情/成交/持仓/订单污染均无泄漏。`zo`公开索引迁移连续执行两次；本项无新DDL，
  `make gen-model`不适用。
- COMBO-001～COMBO-009：50路相同 `clientComboId` 并发全部返回同一父单，只形成1父/2腿/2影子/
  2冻结，同键不同内容拒绝。真实 Asset 冻结已提交后丢响应时保持资金屏障，原指令重试后激活并可
  全额释放；激活前 kill switch 和标的价跳变导致的卖方保证金不足均整组撤销。FOK不足和跨账户STP
  均0腿；MySQL第二腿故障留下0成交/0outbox/0资产指令/0撮合序号，移除故障后形成1成交组、2腿、
  1:2比例、总量3。买方扣冻结提交后丢响应时2个outbox均阻断且持仓0，恢复后7条唯一资金指令
  全成功、2个outbox和4个持仓完成。后台跨租户详情/强撤及空原因拒绝，授权整组强撤资金归零。
  `zp`组合迁移连续执行两次并实际约束撤单状态机；本项无新DDL，`make gen-model`不适用。
- ACCT-002/003：钱包已有100 USDT但风险状态为`RESTRICTED`时，两个不同业务账户的增险请求均在
  订单、客户端键和资金指令生成前拒绝；同用户以两个不同业务账户提交相反可成交订单时执行
  cancel-taker，零成交、零持仓、1条`STP_PREVENTED/SELF_TRADE_PREVENTED`不可变事件。清理maker后
  4条冻结/释放指令和流水全部成功对账，钱包恢复100、冻结0，重放无新增。
- ACCT-004：同一组合保证金用户从业务账户8301/8302同时提交两张卖单，钱包级风险行将准入事务
  串行化；两单引用同一批准配置，组合整体保证金765 USDT只冻结一次增量，不按账户重复抵扣或读取余额。
  撤单后4条冻结/释放指令及流水全部成功对账，冻结/释放各765，钱包恢复1000、冻结0，重放无新增。
- EQ-001/EQ-002：真实 Asset 余额与多/空期权净市值组成净清算权益，不重复计算权利金。
- ASSET-001：冻结已成功但响应丢失时按原业务号重放；迁移前无幂等行的唯一冻结被接管；
  相同业务号修改经济字段和重复冻结证据 fail closed；唯一历史冻结补齐成功幂等账，重复历史
  业务键不被自动认领并输出 `asset_freeze_duplicate` 租户水位，非 Option 重复键不污染该指标。
- 两个业务账户冻结100/200并释放后，4条 Option 指令全部成功且对账一致；Asset 冻结/释放
  各只有2条流水，钱包和 Option 镜像最终均为总额1000、可用1000、冻结0。
- COIN-003：实物 Call 卖方按订单实际 `margin_coin=BTC` 冻结0.25 BTC，实物 Put 卖方按
  `margin_coin=USDT` 冻结300 USDT；实际 Option 控制撤单生成两条释放指令，真实 Asset 执行、
  对账和订单 `CANCELING -> CANCELED` 完成。两币种冻结/释放各1条，重跑不增加流水，钱包及
  `account_id=0` 镜像均恢复到 BTC 1 / USDT 1000。
- COIN-004：实物 Call/Put 均从公开下单入口覆盖 LIMIT、MARKET、POST_ONLY、IOC、FOK；另覆盖
  用户撤单、强平系统撤单、管理强撤和合约到期释放。每类各8张订单、5种订单类型、16条冻结/释放
  指令及16条唯一流水全部成功对账；Call 全程只使用 BTC且冻结/释放各8，Put全程只使用USDT且
  冻结/释放各800。每类7单CANCELED、1单EXPIRED、管理审计1条，重放后钱包冻结均为0。
- PRICE-008：现金 Call 到期窗口内3个不可变快照 `119/120/121` 生成并确认中位数 `120`；
  数据库拒绝写入错误已确认价 `121`，无正式价格的合约保持 `EXPIRED` 且不创建结算。
  使用正式价后真实 Asset 从卖方50 USDT冻结保证金扣20、释放30，并向买方入账20；3条指令
  全部成功且已对账，双方余额为120/80、合计200、冻结0，结算/批次/合约进入终态，生命周期
  和资产任务重放均不增加结算、指令或流水。
- PRICE-009：现金到期以501个不同多头账户和1个空头账户形成502个持仓，跨越持仓查询100条
  实际上限及500条边界；第一个多头数量为0.5，其余为1，总行权量500.5。正式价120、行权价100、
  10%行权费形成1004条资金指令/唯一流水：空头扣10010并释放990，多头净入9009，费用账户入1001；
  借贷各10010，参与钱包总额12000且冻结0，502个持仓全部结算，重放无新增。该场景同时发现并修复
  现金到期读取请求500条但通用分页器规范化为100条后错误提前结束、导致大批量静默漏结算的问题。
- SET-001：在卖方扣冻结 RPC 发出前注入一次 `Unavailable`。失败轮次只把 step 1 指令记为
  `FAILED/retry=1`，买方余额保持100、卖方仍完整冻结50，step 2没有指令成功且没有 Asset流水；
  恢复后沿用原 `instruction_no` 调用真实 Asset，只产生一条扣款流水，再放行买方入账20和卖方
  释放30。另一个场景让真实 Asset 先扣20并落流水再丢失响应：Option仍阻断step 2，重试同一
  指令只认领原流水而不二次扣款。两场景最终均为3条指令成功且对账、余额120/80、合计200、
  冻结0，重跑无重复流水。
- OPS-006：把卖方扣款指令置为已 claim 的超龄 `PROCESSING`，用其原始字段调用真实 Asset 成功
  扣20并落唯一流水，然后模拟执行者在保存结果前终止。恢复前买方仍为100且step 2无成功/流水；
  下一轮任务自动恢复超龄指令并用原 `instruction_no` 认领已有流水，未增加重试计数或重复扣款，
  随后完成入账20、释放30，最终余额120/80、合计200、冻结0。
- OPS-006（真实独立进程强杀与多实例接管）：一个独立 Option 测试工作进程通过真实 Asset RPC
  完成10 USDT冻结后，在收到响应和保存Option结果前被父进程发送`SIGKILL`；数据库保留
  `PROCESSING/retry=0`，Asset已有且仅有1条冻结和1条流水。被杀实例的30秒Redis任务租约不手工
  删除，期间新进程被正常拒绝；租约自然到期后，两个全新工作进程同时竞争，只有一个进入Asset并
  以原业务号认领原流水，另一个得到任务已运行响应。最终指令`SUCCESS/retry=0/MATCHED`，钱包
  `100/90/10`，冻结、流水和进入Asset的接管者均为1。为控制仓库门禁时长，仅在租约真实到期后
  把独立的数据库60秒超龄时间加速到61秒；生产容器编排级重启、实例身份和告警通知仍须预生产签署。
- EX-009（美式行权专用进程强杀）：第一工作进程先完成空头冻结保证金扣40和释放10；第二个独立
  Option进程调用真实Asset完成多头净入36并落唯一流水后，在收到响应和保存Option结果前被
  `SIGKILL`。此时行权/指派仍为`PENDING`、目标指令为`PROCESSING/retry=0`、手续费入账仍为
  `PENDING`。30秒Redis租约自然到期（正式结果`29.963s`）后两个全新进程竞争，只有一个以原
  指令号认领既有多头流水并完成手续费入账。最终行权和唯一指派均`DONE`，4条指令全部
  `SUCCESS/MATCHED`、4条流水唯一，目标入账仍只有1条流水；多头/空头/费用钱包为`136/60/4`，
  总额200、冻结0，长短仓及保证金批次终结，收益分别为`26/-30`（含多头费用4）。
- SET-003：卖方先从冻结金扣50，剩余可用50不足以执行下一笔70扣款；连续20次真实 Asset
  失败后指令进入 `MANUAL_REVIEW`，买方入账 step 始终无成功/流水。缺原因和缺操作人的人工
  重试均被拒绝且保留 `retry_count=20`。以独立业务号补资20后，管理员使用原因
  `SETTLEMENT_BALANCE_TOPUP_VERIFIED` 和 operator 9002 重试原指令；只产生一条70扣款流水，
  随后完成买方入账120。唯一 `ASSET_INSTRUCTION_MANUAL_RETRY` 事件保存原状态、20次失败和错误，
  UPDATE/DELETE 均被触发器拒绝。最终买方220、卖方0、总额220（含外部补资20）、冻结0，
  三条结算指令全部成功且对账。
- EX-001/EX-002/ASN-003/ASN-004：美式现金 Call 的同一 `client_exercise_id` 并发20次，
  只生成1笔行权、一次数量冻结且全部返回同一ID；同键改数量被拒绝。两个并发清算执行只生成
  两个 `(create_times,id)` FIFO 空头指派和6条指令；另以501和5000个独立空头执行真实 Asset
  容量场景，强制跨过每页500的游标边界。卖方平仓与行权竞争另执行1个预置平仓单和10轮
  同时起跑：预置单被`AMERICAN_EXERCISE_ASSIGNMENT`确定性撤销，10轮均为行权先占用仓位、
  平仓按最新数量拒绝；11个指派、33条行权资金指令全部成功，活动平仓单为0。平仓冻结/释放
  与指派统一使用仓位行锁，防止旧快照回写。真实 Asset 完成空头扣120、多头净入108、
  手续费12，6条流水唯一并全部对账。结合开仓权利金30，多头/空头A/空头B收益为
  `78/-30/-60`，加平台费用12合计0；最终钱包`178/70/140/12`、总资产400、冻结0，
  所有仓位和 margin lot 均已结清。重放行权、清算和资金任务不新增记录或流水。
- EX-004/EX-005/EX-007/EX-008/SET-002：混合 AUTO/DNE 到期只让 AUTO 多头行权1张，
  DNE多头结算数量和到期阶段余额变化均为0，空头只按实际行权量承担1张。真实 Asset 的4条指令/
  4条流水全部成功对账，空头扣20=多头净入18+手续费2，批次借贷20/20。结合两名多头各支付权利金10，AUTO/DNE/空头收益为`8/-10/0`，加平台费用2合计0；最终钱包`108/90/200/2`，四方总资产400。
  治理迁移连续执行两次；数据库拒绝经济字段修改、`SUPERSEDED -> ACTIVE` 回退和删除指令历史。
  截止窗口以毫秒墙钟执行为 `[list*1000, cutoff*1000)`；单测覆盖截止前1毫秒接受和截止时刻拒绝。
  真实 MySQL 行锁验收让主动行权在截止前开始、阻塞到实际跨过截止后再释放，结果为0条行权且仓位
  可用1/冻结0；到期指令等待期间把合约从`TRADING`切到`EXPIRED`同样0落库，`PAUSED`对照生成
  1条`ACTIVE`指令，证明两个入口均使用锁后的新状态和新墙钟复核。
  同一持仓20个不同客户端键同时提交 AUTO/DNE/相反指令，真实 MySQL 串行形成版本1～20：20个键和
  20个版本均唯一，根节点加19条替换链接完整，19条`SUPERSEDED`且仅版本20为`ACTIVE`；逐键重放
  均返回原ID/版本/历史状态，不重激活也不新增，同键改变指令类型被拒绝。
- TIME-001～TIME-005：合约使用上市、最后交易、行权截止、到期、交割五个独立时间点。在精确
  `last_trade_time` 边界调用正式关闭逻辑，活动普通单以 `CONTRACT_LAST_TRADE_ENDED` 撤销，合约保持
  `PAUSED`，没有提前行权、结算价锁定或到期批次；最后交易后、截止前仍可提交 DNE。迁移连续安装
  两次，延迟 close-trading 和周期扫描共用行锁与 due identity。脚本输出 `last_trade_boundary=`，
  用于归档合约/订单/指令终态；目标市场时间规则、多实例NTP和生产通知仍不在本门禁证明范围内。
- PNL-001/PNL-002/ASSET-001/ASSET-002（部分平仓）：真实撮合 1 张部分平仓，成交额15、
  买/卖费用`0.6/0.3`；真实 Asset 完成买方扣冻15.6=平仓方净入14.7+平台费用0.9，
  3条指令/3流水全部成功对账。平仓方剩余1张，`trade_realized=5`、`unrealized=5`、
  `fee_paid=1.3`、`total_return=realized_pnl=3.7`；四方钱包合计300、冻结0。资金和持仓任务重跑不新增。
- ORD-001/ACCT-001/EQ-001/PNL-002/ASSET-001/ASSET-002（隔离 Call LIMIT 开仓）：从公开
  `PlaceOrder` 进入开放交易日历准入，卖方冻结逐仓保证金50，买方冻结权利金及费用10.4；随后
  2订单全部成交、1笔成交额10，买/卖费用0.4/0.2。真实 Asset 的5条指令/5条流水全部成功对账，
  outbox/inbox各1条完成，形成2份持仓、1个50保证金lot和2个钱包级NORMAL风险账户；三方钱包
  `89.6+109.8+0.6=200`，其中卖方仍冻结50。重放买方客户端订单号返回原订单，订单、冻结、成交、
  流水、持仓和风险计数均不增加。该证据仅覆盖隔离 Call LIMIT 开仓，不外推到其他订单类型。
- CTRL-001/CTRL-009（并发用户/OI限额）：第一组合约设置多头用户上限10、OI上限100，同一用户
  以20个不同客户端键同时提交1张买开；第二组合约设置用户上限100、OI上限10，由20个不同用户
  同时提交。两个场景均严格为20次`ORDER_CONTROL_EVALUATED`、10张订单受理、10次拒绝；拒绝原因
  分别且仅为`USER_LONG_LIMIT`和`OPEN_INTEREST_LIMIT`。每个受理订单通过真实Asset冻结10.4后从
  公开撤单入口释放，两个场景各20条指令/20条唯一流水全部成功对账，订单和客户端键各10、全部
  CANCELED，0成交/0持仓/0活动单。用户场景钱包回到200，跨用户OI场景20个钱包合计2000，冻结均0。
- ORD-002/ORD-003/ASSET-001/ASSET-002（用户撤单、IOC/FOK）：冻结前用户撤单把待执行冻结指令
  直接标记为CANCELED，不调用Asset且钱包保持100；冻结后用户撤单形成冻结/释放2条成功流水，钱包
  恢复100。IOC买2面对卖1只成交1张，6条资金指令/流水全部成功对账并精确释放剩余10.4，形成
  2持仓、1个50保证金lot和1组outbox/inbox，三方钱包`89.6+109.8+0.6=200`。FOK买2面对卖1保持
  0成交/0持仓，买方20.8与卖方50均全额释放，4指令/4流水成功对账。撤单及客户端订单号重放不新增。
- ORD-004/ORD-005/ASSET-001/ASSET-002（MARKET、POST_ONLY）：MARKET买方以保护价10和最大成交额12
  下单，实际成交1张仅消费10.4并释放剩余1.6；6指令/6流水全部成功对账，形成2持仓、1个50保证金lot
  和1组outbox/inbox，三方钱包保持200。POST_ONLY价格10会主动成交时以`POST_ONLY_WOULD_TAKE`
  零成交撤销；价格8不会主动成交时先进入订单簿，再由公开撤单完成释放。三单共6指令/6流水成功对账，
  0成交/0持仓、三名用户钱包各100/冻结0；客户端订单号重放不新增。
- ORD-002/OPS-003/ASSET-001/ASSET-002（管理强撤与撤单/冻结竞态）：租户管理员9003对同一暂停
  合约中的一笔已冻结订单和一笔待冻结订单执行批量强撤，跨租户请求被拒；两单均以
  `P0_ADMIN_FORCE_CANCEL` 结束，3条指令中2条成功对账、1条在调用Asset前合法取消，两个钱包均
  恢复100/冻结0。每单各写一条不可变 `ADMIN_FORCE_CANCEL_ORDER` 事件，保存操作人和原因，重放
  不新增，UPDATE/DELETE均被数据库拒绝。另连续20轮同时启动一个真实资金任务和两个公开撤单请求：
  每轮恰好一个撤单成功、一个得到业务拒绝，订单最终全部CANCELED、钱包每轮恢复100/冻结0，
  客户端键和指令号均唯一。每轮允许“冻结前取消1条”或“冻结+释放2条成功”两种合法时序，故以
  `canceled + success/2 = 20` 验收，而不把线程调度偶然结果写成固定成功条数。
- LIQ-001/LIQ-002/PNL-002/ASSET-001/ASSET-002（强平）：无穿仓逐仓整仓强平中，原空头保证金
  扣88/释放12，保险账户入账80并冻结80，平台入费8；5指令/5流水全部成功对账，
  四方钱包合计200。原空头`trade_realized=-60`、`fee=8`、`total_return=realized_pnl=-68`并关仓；
  保险接管2张、冻结保证金80；后续风险轮按当前行情把维持保证金刷新为56。重跑无新增。逐仓部分
  强平由风险扫描按`qty_step`选择严格恢复`equity > maintenance`的最小数量，同一钱包一次只允许一笔。
  真实场景从2张中自动选择1张，资金前先把可用/冻结数量改为1/1，并把对应50保证金标为pending；
  5指令/5流水成功对账后源仓剩1张/保证金50/维持保证金28，保险接管1张/保证金40/维持保证金28，
  三方钱包合计120、冻结90，重放不新增。保险接管库存保留风险可见性，但不会递归进入客户强平循环。
- LIQ-003（穿仓与故障恢复）：逐仓整仓清算以用户担保50、保险基金15、平台兜底23覆盖接管80+
  强平费8；4指令/4用户流水、1条保险平台流水和1条兜底平台流水均唯一。保险赔付和平台兜底先后在
  Asset 提交成功后丢失响应，清算准备分别以原业务号恢复；担保扣减提交后丢失响应时，接管入账、
  费用入账和接管冻结均被步骤屏障阻止，原指令重试对账后才继续。最终用户钱包88、平台账户-23，
  合计保持初始65；清算准备`retry=2`、担保指令`retry=1`，重放无新增。
- OPS-004（保险流水语义）：迁移连续执行两次；真实缺口赔付的Option原始金额为15，按类型归一后
  为-15。新流水只接受正业务绝对金额和完整Asset证据，负数新增以及既有流水UPDATE/DELETE均由
  MySQL拒绝；运营工作台和Prometheus使用同一`flow_type + ABS(amount)`表达式。历史行不改写，
  生产逐笔盘点、Asset日终和财务/清算签署仍按
  `docs/option-p1-008-insurance-fund-ledger-repository-acceptance.md`执行。
- 组合保证金账户级清算：每个用户/结算币同时最多一笔，按“移除完整空头后维持保证金下降最多、
  同值取较小position_id”确定仓位；第一笔资金和持仓全部完成后才重新计算整个钱包并创建下一笔。
  两个真实空头仓位顺序完成，8条清算指令和2条最终余量释放指令全部成功对账。每笔不可变保存风险配置ID/版本、
  清算前后维持保证金、剩余初始保证金和抵押前后值，并验证`collateral_after >= initial_after`；
  最后一笔完成后的抵押余量由下一风险轮释放，用户钱包冻结归零。执行前使用当前行情重算钱包；
  正常行情变化时保留触发价，但按触发/当前剩余初始保证金较大值保护抵押。账户已恢复、配置或仓位变化、
  原候选不再降低风险、当前抵押不足时旧记录进入`CANCELED`，不得按失效证据扣款。组合部分数量现按
  `qty_step`逐步调用同一非线性情景模型，要求维持保证金下降、剩余初始保证金受抵押覆盖，并在扣除强平费后
  严格恢复健康；等号不通过，超过100000步fail closed，无部分解则整仓并在完成后重扫。执行前按当前状态
  重新选量，不得套用逐仓线性公式。代码、边界单测和真实 Asset RPC 均已通过：2张自动选择1张，抵押消费44、
  强平费4，4条清算指令全部成功并对账；风险刷新后源仓剩1张/抵押560、钱包`656/96/560`且未创建第二笔。
  恢复门禁另以触发后补资100验证：记录为`CANCELED/retry=0`、0资金指令；规范CHECK及升级迁移均允许状态7，
  且迁移连续执行两次通过。
- `20260801_option_freeze_idempotency_evidence.sql` 连续执行两次；专用覆盖索引唯一存在，唯一
  Option 历史冻结补账1条，重复业务键补账0条，异常租户精确输出1个重复键/最早时间700。
- 实物交割真实 Asset RPC：covered Call 和 covered Put 分别生成1个独立交割单元、4条指令和4条唯一流水，
  全部成功对账。Call 的买方扣款已提交后注入一次响应丢失，后续步骤在原指令对账前保持阻断，
  原业务号恢复后仍只有1条扣款流水；另一个 Call 在买方扣款完成后对卖方冻结担保扣减注入相同故障，
  两条入账腿保持阻断，恢复后该担保扣减也只有1条流水。余额不足 Call 批次生成2个单元、8条指令：余额充足单元先独立完成，
  余额不足单元无后续成功或流水，补资截止后转 `DEFAULTED` 并使批次显式失败；以操作人9005、理由
  `BUYER_ASSET_TOPUP_VERIFIED` 补资并从受控管理入口按原指令恢复后，两单元、批次和结算全部完成。
  进程边界场景再以两个独立 worker 分别完成买方扣款和卖方担保扣减；第三个 worker 在第一条入账
  已落 Asset 流水、另一条入账仍为 `PENDING` 时被真实 `SIGKILL`。30秒 Redis 租约自然到期后，
  两个全新进程竞争且只有一个接管原指令，最终完成事件仅1条。五个故障/恢复合约合计24条指令/24条流水，
  逐币余额、冻结担保、margin lot 和资金守恒均通过，重放无新增。同一违约单元补资后20路并发管理
  重试仅1路成功、19路拒绝，`manual_retry_count=1` 且不可变审计事件仅1条。
- 实物容量边界以501个独立多头仓位和1个501张全额担保空头跨越100条持仓分页，形成501个独立单元、
  2004条资金指令/流水，全部成功对账；501个完成事件唯一，重放无新增。造数/清算/Asset为
  `1.400s/3.866s/1m41.080s`。
- CTRL-005/CTRL-006 使用公开下单和真实 Asset RPC 同时构造同一用户的 `FUNDING/PENDING/PART_FILLED`
  订单。启用 kill switch 后冻结前指令合法取消，其余订单进入控制释放；释放完成前管理员解除被
  `OperationNotAllowed` 拒绝，开关保持开启，新单只以 `USER_KILL_SWITCH` 拒绝。资金全部成功对账后
  管理员带理由解除，后续下单恢复。另以真实行锁阻塞 maker，令启用和 taker 撮合同时争锁；首次验收
  真实暴露 MySQL 1213，现仅对1213/1205执行最多5次退避重试，最终0成交、两单4指令/流水全部释放、
  启用/解除各1次且两钱包均回到200/冻结0。
- CTRL-004 以101笔已完成真实冻结的活动订单跨越100条分页，标记价从10跳至15恰好达到50%阈值。
  合约原子转 `PAUSED`，活动 halt 精确记录101/101/0，101笔全部进入释放；最终202条资金指令/流水
  成功对账，0成交/0持仓，用户钱包回到2000/冻结0。单笔控制撤单失败不会阻断后续订单，halt保存
  精确成功/失败计数和最后错误。
- ERR-001～ERR-004 以一笔保留的原成交、一个真实冻结活动单和三腿平衡现金更正验收：无关账户
  分录在暂停前拒绝；建案后合约暂停且活动单按原业务号释放；申请人自审拒绝，20路独立管理员并发
  复核仅1个批准成功。首笔扣款在 Asset 提交后丢响应仍只有1条权威流水，第二笔余额不足期间 step 2
  始终0成功/0流水；补资后沿用原指令恢复，最终3指令/3流水成功对账、借贷净额0、案件完成，
  原成交全字段哈希不变，创建/批准/完成事件各1。验收库连续两次安装更正不可变触发器。
- MMP-001～MMP-008 以102笔同组真实冻结 `POST_ONLY` maker 报价、另一报价组和普通单验收。
  第1笔 maker 成交在笔数阈值等号触发，1笔成交、其余101笔跨越100条分页全部进入控制释放，
  另一组和普通单在触发时保持 `PENDING`。冷静期已截止的新 MMP 单和人工恢复在任一订单仍为
  `CANCELING` 时均拒绝；一笔解冻在 Asset 提交后丢响应，Option 保持 `FAILED/CANCELING` 而
  Asset 已有唯一流水，管理员沿用原指令恢复后流水仍为1。随后20路并发人工恢复严格仅1路成功，
  创建新 MMP 报价恢复；最终101条控制释放全部成功对账，触发/组撤单/人工恢复事件各1，
  其他组和普通单未被误撤。验收库连续两次安装既有 `zg` MMP 迁移。
- ACT-002～ACT-010 以管理逻辑创建和独立复核 2-for-1 拆分，先证明 `EXPIRING` 订单会阻断复核，
  再迁移5001个持仓。首轮恰好完成100条；第101个空头的`pending_margin=50`使第二轮失败且不留下
  部分后继仓/审计，解除后每轮创建全新任务对象并从持久化游标续跑。最终5001个源仓迁出、5001个
  后继仓持有，数量5001→10002、成本基础50010→50010、逐持仓唯一审计5001、margin lot审计2。
  两个真实Asset冻结合计100和原2条流水保持不变，Option资产指令0；并发风险扫描把钱包置为
  `RESTRICTED/last_calc_time=0`且按钱包/合约去重错误。merger对照强制人工，迁移明细不可改删。
  场景发现并修复资产阻塞查询引用不存在`contract_id`以及风险扫描5001次重复查询/错误的问题。

除501/5000容量和20轮撤单—冻结竞态外的固定场景当前共724条指令：717条成功对账、7条冻结前合法取消。
美式、实物和现金到期容量场景固定增加8513条成功对账指令；竞态每轮可增加1条取消或2条成功指令，
因此全套结果在9257～9277条之间；稳定门禁为无其他状态、所有成功均已对账，且
`success + 2*canceled = 9284`。2026-08-02 纳入 P2-002/P2-003 独立生命周期及 P2-007 复杂订单后，最近正式全套结果为
9277条指令、9270条成功且已对账、7条冻结前合法取消，恰好满足该门禁；公司行动不生成新的
资产指令，最终复跑主测试（含独立最后交易、组合订单和5001迁移）为`113.879s`；
`last_trade_boundary` 实际为合约PAUSED、订单CANCELED、原因正确、五时间独立且提前行权/结算均为0。
新增场景对2张保险接管空头申请退出，只成交1张并撤销IOC余量：20路并发执行仅1个ADMIN订单/
客户端键，剩余仓位1、保证金40，4条关联资金指令/4条Asset流水和1条预算入账唯一。
日历专项另增7条指令：V1→V2未来边界场景2条成功对账；人工 halt 场景
5条为4成功对账+1冻结前合法取消。在 `CANCELING` 资金释放完成前尝试恢复必须失败，
其中一笔解冻在Asset提交后丢响应时保持`FAILED/CANCELING`且只有1条释放流水；第二次恢复仍被拒绝，
管理员留下人工恢复审计并沿用原指令后才允许恢复，流水仍为1。最终钱包回到初始金额，日历和 halt
已批准证据改写/删除均被 MySQL 触发器拒绝。
公开只减仓平仓单与人工halt另从同一并发屏障起跑20轮，并交替给予一方150ms先发优势；正式结果为
10轮先落单后撤销、10轮halt先提交且新单零副作用拒绝。终态仓位可用/冻结均为1/0、资产指令为0、
残单为0，因此不改变资金门禁。
美式501场景造数/清算/Asset为`375ms/2.848s/15.407s`，5000场景为
`3.492s/27.967s/2m53.573s`；实物501场景为`1.745s/4.009s/1m42.487s`，
现金到期501账户场景为`1.110s/1.465s/53.953s`；美式/实物强杀接管中租约自然到期分别约为
`30.165s/29.940s`，通用资金多实例接管总测试为`30.245s`。
开发机波动不作为批准SLA。
该门禁不替代预生产的组合保证金快照失效/破产大样本、复杂订单批准容量长稳及组合专用消息乱序/
容器编排级强杀/重启与告警接管、
强平及实物币种五类普通单/用户撤单和管理强撤全路径、组合保证金容量、保险退出生产角色/限额/通知复演、
同一真实仓位继续行权/到期/强平、生产财务补资
审批/通知、生产告警通知和人工签署。每个真实合约使用
`docs/templates/option-exercise-expiry-control-record.md` 归档行权/到期控制证据。
