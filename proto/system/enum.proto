syntax = "proto3";

package system;

option go_package = "wklive/proto/system;system";

enum SysConfigType {
  UNKNOWN = 0;
  SYSTEM_CORE = 1;    // 系统配置
  OBJECT_STORAGE = 2; // 对象存储配置
  ITICK_CONFIG = 3;   // ITICK配置
}