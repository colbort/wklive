# Inbox/Outbox、Asset 资金指令与 DTM 对比评估

> 文档状态：`ASSESSMENT_COMPLETE / KEEP_CURRENT_CORE_ARCHITECTURE`  
> 评估日期：2026-08-03  
> 适用范围：Trade、Option、Staking、Payment、Asset、Liquidity 及相关消息、对账和管理后台  
> 核心结论：Inbox/Outbox、Asset 资金指令和 DTM 不属于完全相同的技术层次。当前方案更适合交易所核心资金与结算链路，不建议使用 DTM 全量替换。DTM 仅适合后续新增的低频、短周期、跨多个写服务且具备明确补偿动作的流程。

## 1. 评估目标

本次评估回答：

1. 当前 Inbox/Outbox 和 Asset 资金指令分别解决什么问题；
2. DTM 能否替换这些机制；
3. 两种方案在一致性、高并发、审计、恢复和运维方面的差异；
4. 当前方案存在哪些维护缺点；
5. 哪些服务继续使用现有方案，哪些场景可以评估 DTM。

## 2. 总体评价

| 方案 | 当前交易所核心链路适配度 | 结论 |
| --- | ---: | --- |
| Inbox/Outbox + Asset 资金指令 | 8.5/10 | 继续使用并优化 |
| DTM 全量替换 | 3/10 | 不建议 |
| DTM 用于新增短事务 | 7/10 | 满足准入条件后可试点 |

上述分数评价的是对当前系统的架构适配度，不代表 DTM 产品本身的质量。

当前方案的主要优势是领域状态完整、资金可审计、适合长流程、支持人工恢复；主要缺点是各服务存在重复的状态机、租约、重试和恢复代码。

DTM 的主要优势是统一 Saga/TCC 等分布式事务编排，减少通用协调代码；主要缺点是增加关键基础设施，而且不能替代业务流水、资金指令、Kafka 和对账。

## 3. 三种机制解决的问题

### 3.1 Outbox

Outbox 用于保证业务数据库提交与“待发送事件”同时落库：

1. 业务事务提交订单、成交、支付或结算结果；
2. 同一个本地事务写入 Outbox；
3. 后台 Publisher 将事件发送到 Kafka；
4. 发送失败可以安全重试；
5. 下游暂时不可用时不阻塞上游本地事务。

Outbox 解决的是可靠事件发布，不是完整的分布式事务。

### 3.2 Inbox

Inbox 用于消费者接收事件时：

- 根据 `event_no` 或业务唯一键去重；
- 记录处理中、成功和失败状态；
- 支持失败重试和进程重启恢复；
- 防止 Kafka 重投造成重复业务效果。

Inbox 解决的是事件消费幂等和恢复。

### 3.3 Asset 资金指令

Asset 资金指令用于记录具体资金业务步骤：

- 业务单号和指令号；
- 租户、用户、账户、币种和金额；
- 冻结、扣冻结、释放、可用入账或扣减等动作；
- 同一业务内的执行顺序；
- 执行状态、重试次数和错误信息；
- 实际 Asset 流水号；
- 对账状态及人工处理状态。

Asset 指令既是可恢复的业务命令，也是资金审计和对账依据。

### 3.4 DTM

DTM 是独立的分布式事务协调器，可协调：

- Saga 正向操作和反向补偿；
- TCC Try、Confirm、Cancel；
- XA；
- 事务消息；
- Workflow 分支执行和结果重放。

DTM 解决的是多个事务分支如何统一推进、提交或补偿。它不会自动生成业务补偿接口，也不会自动理解订单、仓位、结算和资金含义。

官方参考：

- DTM 架构：<https://en.dtm.pub/practice/arch.html>
- go-zero 支持：<https://en.dtm.pub/ref/gozero.html>
- Workflow：<https://en.dtm.pub/practice/workflow>
- 子事务屏障：<https://en.dtm.pub/practice/barrier.html>

## 4. 执行链路对比

当前方案：

```text
业务本地事务
  ├─ 订单/成交/结算数据
  └─ Outbox 事件
          │
          ▼
        Kafka
          │
          ▼
        Inbox 去重
          │
          ▼
    持久化 Asset 指令
          │
          ▼
    幂等调用 Asset RPC
          │
          ▼
     资金流水与日对账
```

DTM 方案：

```text
业务发起方
    │
    ▼
DTM Server
    ├─ 调用事务分支 A
    ├─ 调用事务分支 B
    └─ 调用事务分支 C
          │
          ▼
  Confirm / Cancel / Saga 补偿
```

当前方案是业务驱动的异步最终一致性；DTM 是协调器驱动的分布式事务。两者可以组合，但在当前系统上直接叠加会产生重复状态和重复运维入口。

## 5. 核心能力对比

| 对比项 | 当前 Inbox/Outbox + Asset 指令 | DTM |
| --- | --- | --- |
| 一致性模式 | 最终一致性 | Saga/TCC/XA/最终一致性 |
| 服务耦合 | 事件解耦，较低 | 协调器需要了解事务分支，较高 |
| Kafka | 原生适合 | 不能直接替代现有事件总线 |
| 长流程 | 适合 | 不适合超长全局事务 |
| 高并发交易 | 更适合异步削峰 | 增加协调器和状态存储开销 |
| 资金审计 | 指令、业务号和流水完整 | 全局事务记录缺少资金语义 |
| 失败恢复 | 自动重试、人工处理、对账 | 自动重试和补偿 |
| 重试上限 | 可转人工处理 | 部分模式倾向继续重试 |
| 业务可见性 | 可看到订单、金额、动作和流水 | 主要看到 GID、分支和事务状态 |
| 幂等 | 业务号及唯一索引 | 仍需幂等或 Branch Barrier |
| 补偿 | 按业务定义冲正 | 仍需业务提供补偿接口 |
| 部署成本 | 使用现有 MySQL、Kafka 和任务系统 | 新增 DTM Server 和高可用存储 |
| 人工恢复 | 当前已经支持 | 需要额外建设业务管理入口 |
| 资金对账 | 当前已经具备 | DTM 不提供领域资金对账 |

## 6. 当前方案的主要优势

### 6.1 资金业务可审计

以 Option 为例，`services/option/option.sql` 中的 `t_option_asset_instruction` 可以回答：

- 为什么发生这笔资金变化；
- 对应哪个订单、成交、持仓、强平或交割单元；
- 应执行什么资金动作；
- 实际产生了哪条 Asset 流水；
- 当前是否已经完成对账；
- 是否需要人工处理。

DTM 的事务分支记录主要描述分支成功、失败、确认或补偿，不能替代上述资金领域证据。

### 6.2 适合长时间业务

当前系统存在分钟、小时甚至跨日流程：

- 永续资金费结算；
- 交割合约结算；
- Option 到期、行权、指派和实物交割；
- 强平、保险基金和 ADL；
- Payment 第三方异步回调；
- Staking 周期收益和到期赎回。

这些业务需要可查询的中间态和人工恢复入口，不适合长期保持一个通用全局事务。

### 6.3 支持失败转人工

Trade、Option、Staking 均支持达到重试上限后进入人工处理，而不是无限重试：

- Trade：`services/trade/internal/logic/task/contract_saga.go`；
- Option：`services/option/internal/logic/task/processassetinstructionslogic.go`；
- Staking：`services/staking/internal/logic/helpers/fund_operation.go`。

在账户状态、配置版本或市场状态已经变化时，暂停并等待审核通常比持续自动执行更安全。

### 6.4 保持事件解耦

Inbox/Outbox 允许生产者和消费者独立部署、扩容与恢复：

- 下游暂时离线不阻塞上游本地事务；
- Kafka 积压后可以继续消费；
- 多个消费者可订阅同一事件；
- 支持事件重放和灾难恢复。

DTM Saga/TCC 通常要求参与服务可被协调器直接回调，运行时耦合更高。

## 7. 当前方案的主要缺点

### 7.1 重复建设

Trade、Option、Staking 分别维护相似的：

- 状态枚举；
- Claim 和执行租约；
- 重试退避；
- 最大重试次数；
- 人工处理状态；
- 错误记录；
- 恢复任务；
- 对账逻辑。

如果缺少统一规范，各服务可能逐渐产生不同的故障处理语义。

### 7.2 缺少统一跨服务关联号

当前同时存在：

- `event_no`；
- `instruction_no`；
- `operation_no`；
- `order_no`；
- `settlement_no`；
- `request_no`。

这些业务号能够保证幂等，但跨服务排障时需要逐表关联。DTM 的 GID 在统一事务追踪方面更直接。

### 7.3 全局进度查看分散

当前需要分别查询业务表、Outbox、Inbox、Asset 指令、Asset 幂等和流水，才能还原完整过程。DTM 自带统一事务状态视图，在通用事务运维方面更集中。

### 7.4 补偿设计不统一

部分业务采用继续执行到成功，部分业务采用冲正，部分业务达到上限后人工处理。需要形成统一的场景分类，避免同类资金操作采用不同恢复原则。

## 8. 按服务评价

### 8.1 Trade

结论：保留当前方案。

Trade 涉及高并发撮合、资金预占、成交结算、资金费、交割、强平和 ADL。结算指令既是恢复状态，也是业务账本的一部分，不适合使用 DTM 事务表替换。

### 8.2 Option

结论：保留当前方案。

Option 行权、指派、保证金、到期结算、实物交割和保险库存退出都需要账户、仓位、保证金批次、资产指令和流水共同对账。DTM 不能简化这些领域状态。

### 8.3 Staking

结论：保留当前资金操作单。

Staking 主要跨 Staking 和 Asset 两个写服务。现有操作单已经记录本金、收益和手续费步骤，专门部署 DTM 的收益有限。

### 8.4 Payment

结论：保留支付流水和 Outbox。

支付依赖第三方异步回调，DTM 无法控制外部支付渠道。渠道请求流水、支付状态、Outbox、Asset 入账和渠道对账仍然必须保留。

### 8.5 Liquidity

结论：可以作为 DTM 候选试点，但暂不实施。

内部做市账户开通需要连续操作 Trade、User、Asset 和 Liquidity，属于低频、多服务写流程。试点前必须补齐用户撤销/禁用、资金冲正、Provider 撤销及各步骤幂等能力。

## 9. 推荐优化方案

不引入 DTM，也可以吸收其统一事务追踪和通用协调的优点。

### 9.1 增加统一关联号

新增统一 `correlation_id` 或 `workflow_id`，贯穿：

```text
业务主记录
  → Outbox
  → Inbox
  → Asset 指令
  → Asset 幂等记录
  → Asset 流水
  → 对账问题
```

业务原有 `order_no`、`instruction_no` 等继续保留，统一关联号只用于跨服务追踪。

### 9.2 统一可靠执行规范

形成跨服务规范，统一：

- 状态命名和终态定义；
- Claim 租约和续期规则；
- 最大重试次数；
- 指数退避算法；
- `MANUAL_REVIEW` 进入条件；
- 确定性业务号生成规则；
- gRPC 超时但下游成功的核实流程；
- 管理员重试权限和审计；
- 对账完成条件；
- 错误码、告警字段及敏感信息保护。

统一规范不等于强行共用一套业务状态机，各服务仍维护自己的领域模型。

### 9.3 抽取技术基础能力

可以抽取不包含业务判断的基础库：

- 租约 Claim/续期；
- 重试退避计算；
- 错误信息截断；
- 业务号格式校验；
- Worker 并发控制；
- 统一指标和告警标签；
- 幂等结果查询辅助方法。

资金动作、步骤顺序、人工处理条件和对账不变量继续由业务服务独立维护。

### 9.4 建设业务链路追踪页面

Admin UI 增加统一“业务链路追踪”能力，输入业务号或关联号后展示：

- 业务主记录；
- Outbox 和投递结果；
- Inbox 和消费结果；
- Asset 指令及每一步状态；
- Asset 幂等记录；
- Asset 流水；
- 对账问题；
- 自动重试和人工操作记录。

该页面可以获得 DTM 全局事务视图的大部分运维价值，同时保留完整业务语义。

### 9.5 增加自动一致性检查

至少持续检查：

1. 已成功 Outbox 是否存在相应 Inbox；
2. 已成功业务是否存在完整 Asset 指令；
3. 成功 Asset 指令是否只对应一条有效流水；
4. 失败或人工处理指令是否超过允许时限；
5. 业务金额、指令金额和 Asset 流水是否守恒；
6. 已完成业务是否仍存在活动租约或处理中指令。

## 10. DTM 适用边界

只有同时满足以下条件才评估 DTM：

1. 新增业务至少跨两个独立写服务；
2. 不能通过单服务本地事务解决；
3. 正常执行时间为秒级或短分钟级；
4. 所有分支都具备确定性幂等键；
5. 所有已提交步骤都有合法、可重复的补偿动作；
6. 不属于撮合、行情、行权、交割、强平等长流程；
7. 具备 DTM 高可用部署、监控和恢复能力；
8. 故障注入证明 DTM 方案比持久操作单更简单、更安全。

任一条件不满足，默认继续使用当前模式。

## 11. 最终决策

1. 不使用 DTM 替换现有 Inbox/Outbox；
2. 不使用 DTM 事务分支记录替换 Asset 资金指令；
3. 不在 Trade、Option、Staking、Payment 核心链路全量接入 DTM；
4. 继续保留业务幂等、资金流水、对账和人工处理；
5. 优先统一关联号、租约、重试、告警和管理查询规范；
6. 抽取通用技术能力，但各服务独立维护业务状态机；
7. DTM 仅用于后续新增且满足准入条件的短事务试点；
8. Liquidity 内部做市账户开通是候选场景，但补偿能力完成前不得实施。

综合评价：当前方案代码量高于使用通用协调器，但更符合交易所高并发、长流程、资金审计、业务对账和人工恢复要求。现阶段优化当前 Inbox/Outbox 与 Asset 指令体系，比全量引入 DTM 风险更低、收益更明确。
